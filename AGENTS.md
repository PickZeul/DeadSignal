# DEAD SIGNAL Repository Guidelines

## Project Identity and Goal

- Game title: `DEAD SIGNAL`
- Development project: `1.44MB GAME_DEV`
- Final goal: complete the game while keeping the entire final submission at or below exactly `1,474,560 bytes`.
- Do not interpret `1.44MB` using an approximate KB/MB display. Record and compare sizes in exact bytes.

This repository is currently a minimal Visual Studio Windows Native C++ project. `DeadSignal.slnx` is the solution entry point and `DeadSignal/DeadSignal.vcxproj` is the single application target. The project currently contains one source file, `DeadSignal/DeadSignal.cpp`. Both Win32 and x64 Debug/Release configurations exist.

## Decision Priority

When rules or materials conflict, use this order:

1. The user's most recent instruction for the current task
2. The latest `1.44MB GAME_DEV 기본 기획서`
3. The actual current source, framework, and build state
4. Older documents and prior work

Do not silently resolve a conflict by changing the project. Report the difference to the user first. Do not change an approved design rule without user approval. If a change appears necessary, follow: identify the problem, explain the reason, propose the change, and wait for the user's decision.

## Current Development Stage

`B00 Empty Executable Baseline` is complete. The measured executable sizes were `9,216 bytes` for MSVC Release Win32/x86 and `10,752 bytes` for MSVC Release x64.

`B01 Minimal Win32 Window` is complete. The measured executable sizes were `10,240 bytes` for MSVC Release Win32/x86 (`+1,024 bytes` from B00) and `12,288 bytes` for MSVC Release x64 (`+1,536 bytes` from B00). Both architectures passed build and manual execution validation. Window display, movement, resizing, minimize/maximize, and X-button exit behaved normally. The Release subsystem is Windows GUI and the user entry point is `wWinMain`.

`B02 Minimal Software Framebuffer Output` is complete. The test framebuffer is `2x2 pixels` (`초기 기준`) using 32-bit top-down `BI_RGB`, with a red/green/blue/white test pattern. The Win32 GDI `StretchDIBits` output path is an initial prototype candidate and is not the final Renderer specification.

The measured B02 executable sizes were `10,752 bytes` for MSVC Release Win32/x86 (`+512 bytes` from B01) and `12,288 bytes` for MSVC Release x64 (`+0 bytes` from B01). Both architectures passed build and execution validation. Framebuffer output succeeded, resizing stretched the fixed framebuffer across the full client area, and minimize/maximize and X-button exit behaved normally. The system import `GDI32.dll` was added; no external library was added.

`B03 Minimal Real-time Game Loop` is complete. The blocking `GetMessageW` loop was replaced with non-blocking `PeekMessageW(PM_REMOVE)` message processing. High-resolution time measurement uses `QueryPerformanceFrequency` and `QueryPerformanceCounter`. The prototype Update target is approximately 60 Hz (`초기 기준`); current pacing uses `Sleep(1)` with `timeBeginPeriod(1)` and `timeEndPeriod(1)`. The measured Update rate is approximately 56 Hz and is not the final FPS or timing policy.

The B03 test Update changes the framebuffer red component over time and requests redraw with `InvalidateRect`, reusing the existing `WM_PAINT` and `StretchDIBits` output path. The system import `WINMM.dll` was added; no external library was added.

The measured B03 executable sizes were `11,264 bytes` for MSVC Release Win32/x86 (`+512 bytes` from B02) and `13,312 bytes` for MSVC Release x64 (`+1,024 bytes` from B02). Both architectures passed build and execution validation, including continuous Update, window movement, resizing, minimize/maximize, and X-button exit.

`B04 Minimal Keyboard Input` is complete. The only test input is the W key. `WM_KEYDOWN` and `WM_KEYUP` record pressed/released state in one minimal `bool`; no Input abstraction was added. The framebuffer test color changes correctly while W is pressed or held and returns to its original color when W is released. The B03 real-time loop and timing continue to operate normally. Player, Movement, and full WASD input are not implemented.

The measured B04 executable sizes were `11,776 bytes` for MSVC Release Win32/x86 (`+512 bytes` from B03) and `13,312 bytes` for MSVC Release x64 (`+0 bytes` from B03). Both architectures passed build and execution validation, including W press/hold/release, window movement, resizing, minimize/maximize, and X-button exit. No system import or external library was added.

`B05 Minimal Generated Audio` is complete. A square-wave PCM tone is generated in RAM and played without an audio asset. The prototype values (`초기 기준`) are 8,000 Hz, unsigned 8-bit PCM, mono, 440 Hz, 250 ms, and a 2,000-byte PCM buffer. These values are not final game sound specifications. The tone uses callback-free asynchronous `waveOut` playback with `CALLBACK_NULL`. A new Space press plays one tone; additional Space input is ignored while it is playing.

No Audio Thread, Mixer, SoundManager, or Audio abstraction was added, and no Audio Asset exists. The existing `WINMM.dll` dependency is reused with no new DLL dependency or external library.

The measured B05 executable sizes were `11,776 bytes` for MSVC Release Win32/x86 (`+0 bytes` from B04) and `13,312 bytes` for MSVC Release x64 (`+0 bytes` from B04). Both architectures passed build and execution validation. Tone playback and replay after completion succeeded. During playback, the game loop, framebuffer Update, W input, window movement, resizing, minimize/maximize, and X-button exit continued to operate normally.

`P00 Minimal Play Space` is complete. The Logical Resolution is `320x180` (`확정`), the Aspect Ratio is `16:9` (`확정`), and one logical pixel equals one game unit. The current coordinate convention uses the top-left as `(0,0)`, +X to the right, and +Y downward.

The framebuffer is 32-bit top-down `BI_RGB` and uses `230,400 bytes` of runtime RAM as zero-initialized static storage. Large pixel data is not stored in the executable; the required test pixels are generated at runtime. The P00 test screen contains a solid background, directional top/bottom/left/right borders, a center cross, and small markers for the existing real-time and input validation.

The framebuffer is aspect-fitted to the Window client area without distortion, unused space is black, and the framebuffer is centered. Integer scaling and the final pixel-perfect policy remain `미정`.

The measured P00 executable sizes were `12,288 bytes` for MSVC Release Win32/x86 (`+512 bytes` from B05) and `15,872 bytes` for MSVC Release x64 (`+2,560 bytes` from B05). Both architectures passed build and execution validation. The `320x180` framebuffer, 4:3 and wide aspect-ratio preservation, centering, resizing, minimize/maximize, X-button exit, B03 real-time loop, B04 W input, and B05 generated audio operated normally. No external Asset or new abstraction/system was added. Movement, Collision, and Enemy are not implemented.

`P01 Minimal Player Display` is complete. The Player Visual Bounding Size is `8x12 logical pixels` (`확정`). The Player is currently fixed at the framebuffer center `(160,90)`, with drawing bounds X=`156..163` and Y=`84..95`. A two-color prototype pixel silhouette distinguishing the head, body, arms, and legs is generated at runtime without an external Asset, Sprite System, Renderer abstraction, or Animation. This is not the final pixel art or palette.

The measured P01 executable sizes were `12,288 bytes` for MSVC Release Win32/x86 (`+0 bytes` from P00) and `16,384 bytes` for MSVC Release x64 (`+512 bytes` from P00). Both architectures passed build and execution validation. The `8x12` bounds and centered display remained correct at 4:3 and wide Window sizes, and the existing real-time loop, W input, Space generated audio, resizing, minimize/maximize, and close behavior continued to operate normally. Player Movement is not implemented.

`P02 Minimal Player Movement` is complete. The Player Visual Bounding Size remains `8x12 logical pixels` (`확정`), and the initial position is the logical framebuffer center `(160,90)`. WASD provides free directional movement, with each key state stored in a minimal `bool` and the current Player position stored as `float playerX` and `float playerY`. Movement uses QPC delta time for frame-rate independence. Simultaneous movement on both axes applies `0.70710678f` normalization, while W+S and A+D cancel on their respective axes. Player drawing retains framebuffer bounds checks without implementing a Boundary clamp or Collision.

The prototype movement speed is `60 logical pixels per second` (`초기 기준`). It currently feels appropriate to the user, but it is not the final game movement-speed specification and may change when Sneak / Walk / Run is designed.

The measured P02 executable sizes were `13,824 bytes` for MSVC Release Win32/x86 (`+1,536 bytes` from P01) and `16,896 bytes` for MSVC Release x64 (`+512 bytes` from P01). Both architectures passed Release build and execution validation. Four-direction WASD movement, all four diagonals, opposing-direction cancellation, and near-equal straight/diagonal movement speed operated normally. The real-time loop, generated audio, 16:9 aspect-fit, resizing, window movement, minimize/maximize, and X-button exit also remained normal. No external Asset or Library, new abstraction/system, or Release setting change was added.

P02 does not implement Boundary clamp, Wall Collision, Hitbox/Collision specifications, Sneak / Walk / Run, Acceleration / Deceleration, Facing / Animation, Dash, Slash, or Execute.

`P03 Minimal Boundary + Wall Collision` is complete. The Player Visual Bounding Size and Player Collision Bounding Size are both `8x12 logical pixels` (`확정`) and use the same full rectangle. There is no separate Hitbox and no pixel-perfect silhouette collision. The existing center-based `float playerX` and `float playerY` position remain in use. Screen boundary handling keeps the full Player rectangle inside the `320x180` logical framebuffer, allowing Player center coordinates X=`4..316` and Y=`6..174`.

A single runtime-generated rectangular test Wall uses X=`200..207` and Y=`60..119`, or `8x60 logical pixels` (`초기 기준`). Its visual and collision rectangles are identical. Player movement is tested separately on the X and Y axes with direct rectangle comparisons, which preserves movement on an unblocked axis and allows sliding along the Wall. No CollisionManager, Physics system, Collider/AABB/Hitbox class, multiple-Wall system, or continuous collision detection was added. The test Wall dimensions are not the final game Wall or Tile specification.

The measured P03 executable sizes were `14,336 bytes` for MSVC Release Win32/x86 (`+512 bytes` from P02) and `18,944 bytes` for MSVC Release x64 (`+2,048 bytes` from P02). Both architectures passed Release build and execution validation. The four screen boundaries, movement along each boundary, Wall collision from all four directions, diagonal Wall sliding, matching visual/collision bounds, WASD movement, real-time loop, generated audio, 16:9 aspect-fit, resizing, window movement, minimize/maximize, and X-button exit operated normally. No external Asset or Library, new abstraction/system, or Release setting change was added.

`P04 Minimal Enemy Display` is complete. One static prototype Enemy is displayed with an `8x12 logical pixel` visual size (`초기 기준`) centered at `(120,90)` (`초기 기준`), with drawing bounds X=`116..123` and Y=`84..95`. The Enemy uses a runtime-generated two-color pixel silhouette and no external Enemy Sprite Asset. The prototype size is an initial candidate for comparison with the Player and is not the final Enemy visual size.

P04 adds no Enemy AI, Movement, Patrol, Facing, Animation, Collision, Hitbox, Player-Enemy collision, Vision/FOV, Detection, or combat behavior. The Player can pass through the Enemy and this is expected at P04. The measured P04 executable sizes were `14,336 bytes` for MSVC Release Win32/x86 (`+0 bytes` from P03) and `19,968 bytes` for MSVC Release x64 (`+1,024 bytes` from P03). Both architectures passed Release build and execution validation. Enemy display and Player pass-through worked normally, and the existing Player movement, screen boundary, Wall collision/sliding, real-time loop, generated audio, 16:9 aspect-fit, resizing, window movement, minimize/maximize, and X-button exit remained normal. No external Asset or Library, new abstraction/system, or Release setting change was added.

`P05 Minimal Enemy Vision / FOV` is complete. The static prototype Enemy currently faces right toward +X (`초기 기준`). Its prototype FOV uses a range of `70 logical pixels` and a total angle of `55 degrees` (`초기 기준`); these values are not final Enemy-specific vision specifications. A dark-blue triangular cone is generated procedurally in the runtime framebuffer using direct `dx`/`dy` relationships and the half-angle slope `0.520567f`, without a Vector, Angle, FOV, Vision, or Renderer abstraction.

The single `8x60 logical pixel` test Wall now uses X=`170..177` and Y=`60..119` (`초기 기준`) so it intersects the prototype FOV. This position and size are not final Wall or Tile specifications. P05 retains the Player-center binary test and adds only the direct line calculation required for the fixed +X-facing Enemy and this single vertical Wall. A Player behind the Wall can be inside the raw FOV but is visible only when the Enemy-to-Player line does not cross the Wall. The procedural FOV pixels are clipped by the same Wall calculation, and the small marker is bright only for the final visible result. Player-center testing is still an initial validation method and is not the final Detection rule.

There is no generic Raycast, Line-of-Sight, Vision, or multiple-obstacle system. There is also no Detection accumulation, timer, question/exclamation mark, CALM/SUSPICION/ALERT state, color progression, or Enemy AI. The finalized P05 executable sizes are `14,848 bytes` for MSVC Release Win32/x86 (`+0 bytes` from the pre-occlusion P05 and `+512 bytes` from P04) and `19,968 bytes` for MSVC Release x64 (`-512 bytes` from the pre-occlusion P05 and `+0 bytes` from P04). Both architectures passed Release build and execution validation. The Wall-front case produced raw-inside=true, occluded=false, visible=true; the Wall-behind case produced raw-inside=true, occluded=true, visible=false. Angle/range/behind-Enemy cases, Wall-clipped FOV rendering, the binary marker, Player-Wall collision/sliding, movement, screen boundary, static Enemy display, real-time loop, generated audio, 16:9 aspect-fit, resizing, window movement, minimize/maximize, and X-button exit all behaved normally. No external Asset or Library, new abstraction/system, or Release setting change was added.

`P06 Minimal Detection State` is complete. The existing P05 final `playerInVision` result is reused directly as the only Detection input, without duplicating or abstracting the raw FOV, single-Wall Occlusion, or final visibility calculation. The minimal state is stored as Detection progress, lost-sight elapsed time, and a bool representing the current ALERT state. The state flow is CALM -> SUSPICION -> ALERT -> SUSPICION -> CALM. Visible time fills Detection from `0.0` to `1.0` in approximately `3.0 seconds` (`초기 기준`), equivalent to a fill rate of `1/3 per second`. Lost sight preserves the current progress and state for approximately `0.5 seconds` (`초기 기준`), then decays at the same `1/3 per second` rate, so full decay from `1.0` to `0.0` takes approximately `3.0 seconds` (`초기 기준`). An ALERT Enemy remains ALERT with full progress during the hold, then returns to SUSPICION when decay begins. Progress reaching zero returns to CALM. Reacquiring sight during ALERT hold resets only the lost-sight elapsed time and preserves ALERT; reacquiring during SUSPICION hold or decay resumes filling from the remaining progress and can enter ALERT again. ALERT is not permanently latched and cannot transition directly to CALM.

SUSPICION displays a minimal runtime-generated yellow `?` and ALERT displays a minimal runtime-generated red `!` near the Enemy using direct `3x5` framebuffer pixel patterns. CALM displays no Detection symbol. The FOV uses `redDistance = 70 * detectionProgress`: candidate pixels at or closer than that forward distance are dark red, while farther pixels retain the existing dark blue. CALM therefore has an entirely blue FOV, SUSPICION fills red outward from the Enemy, and ALERT has an entirely red FOV. Lost-sight hold preserves the red extent; decay shrinks it from the far end until the FOV is entirely blue at CALM. Both colors use the unchanged single-Wall clipping calculation. No gradient system, alpha blending, Detection bar, percentage UI, external Asset, generic StateMachine/Manager, Enemy AI, Movement, Chase, or Search was added.

The finalized P06 executable sizes are `15,360 bytes` for MSVC Release Win32/x86 (`+0 bytes` from the previous P06 and `+512 bytes` from P05) and `20,480 bytes` for MSVC Release x64 (`+0 bytes` from the previous P06 and `+512 bytes` from P05). Both solution platforms passed Release Rebuild and execution validation. Continuous visible Detection reached ALERT in approximately `2.991 seconds` on x86 and `2.994 seconds` on x64. ALERT returned to SUSPICION after approximately `0.503 seconds` lost sight on x86 and `0.505 seconds` on x64. Separate x86 runs validated CALM blue FOV, progress-based red fill, unchanged red extent during SUSPICION and ALERT hold, ALERT-hold reacquisition, shrinking red extent during decay, decay reacquisition and ALERT re-entry, the full ALERT -> SUSPICION -> CALM flow in approximately `3.509 seconds` after lost sight, both procedural symbols, and identical Wall clipping for both FOV colors. A Wall-occluded raw-FOV position at approximately `(186,89)` produced final visibility=false and followed ALERT -> SUSPICION -> CALM. Existing movement, collision/sliding, audio trigger, real-time loop, aspect-fit, resize, minimize/maximize, and close paths remained normal. No external Library, new import, new source/header, or Release setting change was added.

`P07-A Minimal Player Facing + Slash` is complete. Player Facing is stored as two signed axis values, starts facing Right (`초기 기준`), uses the existing WASD cancellation result for all eight directions, and retains the last valid direction after input is released. A new J press produces one Slash while held-key auto-repeat is ignored. The prototype Slash uses a direct direction-dependent `8x8 logical pixel` axis-aligned rectangle extending `8 logical pixels` from the relevant Player visual edge (`초기 기준`), a runtime-generated outline lasting approximately `0.10 seconds` (`초기 기준`), and an approximately `0.5-second` cooldown (`초기 기준`). Overlap with the static Enemy prototype rectangle produces only a brief white Enemy feedback. Enemy HP, Damage, Death, a generic Combat/Attack/Hitbox system, Wall attack blocking, and Slash-triggered Detection do not exist. The measured P07-A executable sizes are `15,872 bytes` for MSVC Release Win32/x86 (`+512 bytes` from P06) and `20,992 bytes` for MSVC Release x64 (`+512 bytes` from P06). No external Library, new import, new source/header, or Release setting change was added.

`P07-B Minimal Dash` is complete. A new K press starts one Dash while held-key auto-repeat is ignored. The Dash direction copies the existing eight-direction Facing after the current WASD cancellation result has updated it, so active movement input takes priority and stopped input uses the last Facing. The direction remains fixed for the Dash. Prototype values are a total distance of approximately `32 logical pixels`, a duration of approximately `0.12 seconds`, a speed of approximately `266.67 logical pixels per second`, and a cooldown of approximately `1.0 second` (all `초기 기준`, not final Dash specifications). Diagonal Dash uses the existing `0.70710678f` normalization concept. Active Dash replaces rather than adds to the normal `60 logical pixels per second` movement, and normal movement resumes on the following update. Each Dash update is split into axis displacements below one logical pixel before repeatedly applying the existing X/Y screen-boundary and single-Wall collision checks, preserving Wall blocking and diagonal sliding without a generic CCD, swept AABB, Physics, or Movement system.

Dash feedback consists only of a brighter Player body during Dash movement. Dash causes no Damage, Enemy Hit, invulnerability, dodge window, charge, stock, multi-Dash, Hearing, Noise, or direct Detection/ALERT change. Slash input during an active Dash update is ignored; no Dash-Slash combo or cancel system was added. Runtime validation measured straight Dash at approximately `32 logical pixels`, diagonal Dash at approximately `32.53 logical pixels`, and bright feedback at approximately `0.112 seconds`. Current input and stopped Last Facing covered all eight directions; direction remained fixed after mid-Dash input changes. Boundary Dash remained at Player center X=`4`; a forward Dash stopped at the Wall at center X=`166`; and a diagonal Wall Dash stopped on X while sliding on Y without tunneling. Cooldown input produced zero movement before one second and a new 32-pixel Dash afterward; held-key auto-repeat also produced zero additional movement. Dash ended into normal movement, J Slash remained available afterward, position-based Vision/Detection transitioned normally, and audio, resize, minimize/maximize, close, and x64 Dash smoke tests passed. The measured P07-B executable sizes are `16,384 bytes` for MSVC Release Win32/x86 (`+512 bytes` from P07-A) and `23,552 bytes` for MSVC Release x64 (`+2,560 bytes` from P07-A). No external Library, new import, new source/header, or Release setting change was added.

`P07-C Minimal Execute` is complete. A new L press attempts one Execute while held-key auto-repeat is ignored. The existing P06 Detection state is reused directly: the single prototype Enemy must be alive, `detectionProgress` must be zero (CALM), and the direct Execute AABB must overlap the Enemy rectangle. SUSPICION, ALERT, an already removed Enemy, an out-of-range Player, and incorrect Facing all leave the Enemy unchanged. No separate Detection/FOV calculation or Execute state was added.

Execute reuses the existing Last Facing after the current WASD cancellation result has updated it and supports all eight directions. Its prototype AABB extends approximately `4 logical pixels` from the relevant Player visual/collision edge (`초기 기준`), shorter than the Slash reach. This is only the P07-C validation value; the final Execute `0.4W` logical-pixel conversion rule remains `미정`. A successful Execute sets one minimal `enemyAlive` bool to false, immediately removes the Enemy, and disables its FOV, Detection calculation, `?` / `!`, and binary vision marker. A bright runtime-generated `3x3` marker remains at the removal position for approximately `0.10 seconds` (`초기 기준`). The successful kill directly resets the existing Slash and Dash prototype cooldown values. Execute has no dedicated cooldown. General Enemy HP, Damage, Death, Combat, Execute, or Entity systems and Enemy AI remain absent.

Release Win32/x86 and x64 Rebuilds succeeded. Runtime validation covered CALM out-of-range and incorrect-Facing failures, CALM success, SUSPICION and ALERT rejection, all eight Execute AABB directions using representative coordinates, Enemy/Vision/Detection/symbol/marker removal, L auto-repeat and post-removal re-entry safety, successful Slash/Dash cooldown resets, no reset after failed Execute, and continued Slash, Dash, boundary/Wall blocking and diagonal sliding, Movement, Detection, generated-audio trigger, aspect-fit resize, minimize/restore, and close behavior. The measured P07-C executable sizes are `16,896 bytes` for MSVC Release Win32/x86 (`+512 bytes` from P07-B) and `24,064 bytes` for MSVC Release x64 (`+512 bytes` from P07-B). No external Library, new import, new source/header, abstraction/system, or Release setting change was added.

The current confirmed gameplay input mapping is Arrow Keys for Player Movement, Z for Slash, X for Dash, and C for Execute. The previous WASD, J, K, and L gameplay inputs are retired and no longer handled. Title Menu Navigation uses Arrow Keys and Title Menu Confirm uses Z.

The MVP production phase has started. `M0 Minimal Title / Entry Flow` is complete. The program starts at Title Main with `게임 시작`, `이어하기`, `설정`, and `종료`; `게임 시작` opens the `새 게임`, `불러오기`, and `뒤로` submenu. `새 게임` resets the current prototype runtime and enters Gameplay. Continue and Load perform no File I/O because the Save System is not implemented, Settings remains unimplemented, and Exit uses the normal Window close flow. Gameplay Update is stopped while Title is active, and the confirming Z press and held Title direction keys are consumed by blocking Gameplay input until they are released. Detailed Title/UI design remains `미정`. The measured M0 executable sizes were `18,944 bytes` for MSVC Release Win32/x86 and `26,112 bytes` for MSVC Release x64.

`M1 Final Enemy Gameplay` is complete for the single current prototype Enemy. Its center remains a runtime `float` position starting at `(120,90)`, and the existing `8x12 logical pixel` visual rectangle remains its MVP prototype collision footprint (`초기 기준`, not a final Enemy-specific specification). Enemy Vision now uses one minimal continuous `float` Facing angle; this does not change the Player's existing eight-direction Facing. CALM retains the horizontal `(80,90)` to `(140,90)` Patrol at approximately `24 logical pixels per second` (`초기 기준`) and preserves the pre-SUSPICION Patrol direction/target. After Detection decays to CALM away from the route, the Enemy walks to the nearest point on that Patrol segment without teleporting, using the fixed path buffer when the Wall requires a detour, and then resumes the preserved Patrol direction.

SUSPICION stops Patrol while the Player is visible, stores the visible Player position directly in two `float` last-seen values every Update, and turns the Vision center toward the Player at approximately `120 degrees per second` (`초기 기준`) rather than snapping. When sight is lost, the Enemy walks toward the last-seen position at the existing `24 logical pixels per second` while the existing approximately `0.5-second` hold and approximately `3-second` full Detection decay continue. Reacquisition resumes Fill from the remaining progress. Progress reaching zero enters CALM and begins the Patrol-route return. CALM retains a fully blue FOV; SUSPICION retains the progress-based red/blue FOV and `?`, including the existing single-Wall Occlusion.

Full Detection enters ALERT, hides the entire FOV visual, removes `?`, keeps `!`, and uses `58 logical pixels per second` (`초기 기준`) for both Direct Chase and Search movement, two pixels per second below the Player's current `60`. ALERT does not use the 55-degree cone to reacquire: Direct Chase requires distance at or below `42 logical pixels`, which is 60% of the existing `70` range, plus clear single-Wall LOS. Successful reacquisition discards the current Search/Scan, resets the ALERT failure timer, faces the current Player position, and directly chases it. Losing either the range or LOS condition starts or continues Search and accumulates only failed-reacquisition time. Approximately `10 seconds` without reacquisition ends ALERT as ALERT -> SUSPICION, restores the progress-based FOV and `?`, and begins Detection decay; it never transitions directly to CALM.

ALERT Search uses one fixed `8 logical pixel` navigation cell grid with `40x22` Enemy-center cells (`880` nodes), four-direction neighbors, and `6,160 bytes` of zero-initialized runtime parent/score/state/path buffers. Cells are valid only when the current `8x12` Enemy footprint fits inside the `320x180` play area without overlapping the single test Wall. A minimal local linear-scan A* implementation chooses and retains one pseudo-random valid target approximately `10 logical pixels` from the Player (`10` horizontally or `12` vertically), follows the generated path while retaining the existing axis-separated Boundary, Wall, and Player-contact collision checks, and never regenerates the target before arrival. At arrival the Enemy stops for a `2.0-second` Scan using the arrival Facing as its base: front at `0.0`, left approximately `27.5 degrees` at `0.5`, front at `1.0`, right approximately `27.5 degrees` at `1.5`, and front at `2.0` seconds. Both sides use exactly one second and the same constant angular speed. Reacquisition is checked every Scan Update and immediately resumes Direct Chase; otherwise Scan completion selects a new current-Player-adjacent target and runs A* again. This is fixed prototype navigation for the current play space and single Wall, not a Navigation system or final Enemy-specific AI specification.

New Game resets Enemy position, Patrol direction/return, continuous Facing, Detection, last-seen values, ALERT failure timer, pseudo-random state, Search target/path indices, Scan elapsed/base Facing, and Direct Chase/Search state. Execute still succeeds only in CALM; removal immediately stops Enemy AI, FOV, Detection, Search, symbols, and the binary marker. Release Win32/x86 and x64 Rebuilds succeeded with zero warnings and zero errors. Runtime validation covered Title/New Game, SUSPICION `?` and progress FOV, ALERT `!` with no visible cone, Direct Chase/contact, Search movement, generated audio, aspect-fit resize, minimize/restore, and normal close. Formula/grid validation covered non-snap `120 degrees per second` turning, symmetric Scan checkpoints, target retention/retarget branches, the approximately `10-second` failed-reacquisition transition, and a representative Wall-crossing A* route with zero blocked nodes. Existing Slash, Dash, CALM-only Execute, Boundary/Wall collision, and Enemy-removal branches remain intact in the shared movement/combat paths. The pre-final M1 executable sizes were `22,016 bytes` for MSVC Release Win32/x86 and `30,208 bytes` for MSVC Release x64. The measured M1 Final sizes are `26,624 bytes` for x86 (`+4,608 bytes`) and `33,792 bytes` for x64 (`+3,584 bytes`). Continuous Facing adds CRT math calls (`sinf`, `cosf`, `atan2f`, and `sqrtf`) through the system `api-ms-win-crt-math-l1-1-0.dll`; no external Library, new source/header, generic StateMachine/AI/Navigation framework, or Release setting change was added.

`M2 Minimal HP / Damage / Death` is complete. New Game initializes the Player to `10 HP` and alive and the Enemy to `3 HP` and alive. Each successful Slash overlap subtracts one Enemy HP, reuses the existing approximately `0.10-second` bright Enemy hit feedback, and the third hit removes the Enemy and stops its visual, FOV, Detection, AI, Search, and symbols. A CALM Execute still removes the Enemy immediately regardless of HP, while SUSPICION/ALERT Execute still fails. Both Slash death and Execute removal reset the existing Slash and Dash cooldowns. Dash causes no Damage.

Only an ALERT Enemy can attack. The existing Enemy-versus-Player contact stop is reused with a `1 logical pixel` attack tolerance for its sub-pixel pre-overlap stopping position. Contact subtracts one Player HP at most once per prototype `1.0-second` cooldown and gives approximately `0.10 seconds` of bright Player feedback; CALM and SUSPICION contact cause no Damage. The tenth hit sets the Player dead, cancels active/requested movement and combat, stops Enemy AI/combat updates, dims the Player, and draws a minimal procedural `DEAD` marker. No Respawn, Run End, Game Over flow, HP UI, Enemy HP bar, Attack animation, weapon/hitbox system, or general Health/Damage/Combat system was added. Release Win32/x86 and x64 Rebuilds succeeded with zero warnings and zero errors. Runtime validation on x64 covered one-hit feedback, three-Slash Enemy removal, Dash causing no Damage, SUSPICION Execute rejection, CALM Execute instant removal, ALERT contact cooldown, ten-hit Player death, post-death input blocking, Enemy-AI stop, Title/New Game, Audio, resize/minimize/restore, and normal close. The x86 executable rebuilt successfully but direct execution was blocked by the current Windows Application Control policy. The measured M2 executable sizes are `27,648 bytes` for MSVC Release Win32/x86 (`+1,024 bytes` from M1 Final) and `34,816 bytes` for MSVC Release x64 (`+1,024 bytes` from M1 Final). No new source/header, external Library, abstraction/system, or Release setting change was added.

`M3 Minimal Room Complete` is complete. The current prototype Exit is one runtime-generated rectangle at X=`304..319` and Y=`78..101`, or `16x24 logical pixels` (`초기 기준`, not a final Room specification). It is dark and locked while the Enemy is alive. Slash death or CALM Execute removal unlocks and brightens it without changing the existing Enemy-removal logic. If the Player dies first, the Exit remains locked and Room Complete cannot occur. Overlap between the existing `8x12 logical pixel` Player rectangle and the unlocked Exit sets the minimal `roomComplete` bool; locked overlap does not complete the Room and adds no Door collision. Room Complete stops Player movement and Slash/Dash/Execute input and draws a runtime-generated `CLEAR` marker. It does not load or generate another Room and does not return to Title. New Game resets both `exitUnlocked` and `roomComplete` to false while restoring the existing M2 Enemy and Player state. Release Win32/x86 and x64 Rebuilds succeeded with zero warnings and zero errors. The measured M3 executable sizes are `27,648 bytes` for MSVC Release Win32/x86 (`+0 bytes` from M2) and `35,840 bytes` for MSVC Release x64 (`+1,024 bytes` from M2). No new source/header, external Library, Room/Door/Scene abstraction, or Release setting change was added.

`M4 Minimal Room Sequence` is complete with exactly two fixed prototype Rooms. Room 1 retains the M3 layout. Room 2 starts the Player at `(40,90)`, starts the Enemy at `(220,90)`, patrols over X=`200..260` at Y=`90`, uses one Wall at X=`152..159` and Y=`48..131`, and reuses the Exit at X=`304..319` and Y=`78..101` (all `초기 기준`, not final Room specifications). Clearing Room 1 increments the minimal `currentRoom` value and resets the existing runtime state in the same Gameplay state: Player position/Facing/`10 HP`, Enemy position/Facing/Patrol/`3 HP`, Detection/ALERT/last-seen state, Search/A* path indices and Scan state, combat requests/cooldowns/feedback, and locked Exit state. No Room 1 runtime state remains active in Room 2. Clearing Room 2 sets the minimal `sequenceComplete` bool, stops Player movement and Slash/Dash/Execute input, and draws a runtime-generated `RUN CLEAR` marker. Player death cannot advance either Room. New Game always selects Room 1 and resets `sequenceComplete` and all gameplay state. There is no Procedural Generation, Seed, Reward, Room Manager, or Scene System. Release Win32/x86 and x64 Rebuilds succeeded with zero warnings and zero errors. The measured M4 executable sizes are `28,672 bytes` for MSVC Release Win32/x86 (`+1,024 bytes` from M3) and `36,352 bytes` for MSVC Release x64 (`+512 bytes` from M3). No new source/header, external Library, abstraction/system, or Release setting change was added.

`M5 Minimal Upgrade` is complete. Clearing Room 2 now opens one minimal `UPGRADE` menu before sequence completion instead of immediately displaying `RUN CLEAR`. It presents two distinct choices from the three-item prototype pool `MOVE+`, `SLASH+`, and `DASH+`, plus one Reroll entry. Up/Down Arrow changes the selection and Z confirms by reusing the existing minimal Title input style without a UI/Menu or Upgrade framework. `MOVE+` changes the current runtime Player speed from `60` to `66 logical pixels per second`; `SLASH+` changes the prototype Slash cooldown from `0.5` to `0.4 seconds`; and `DASH+` changes the prototype Dash cooldown from `1.0` to `0.8 seconds` (all `초기 기준`, MVP validation values rather than final Upgrade design). A minimal fixed-seed pseudo-random state produces two different IDs. Reroll can be used once per Run, replaces the pair with a different pair whose IDs are also distinct, and then remains visibly disabled. Selecting either offered Upgrade applies its runtime value immediately, closes the menu, sets `sequenceComplete`, and displays the existing procedural `RUN CLEAR`; there is no next Room. Gameplay movement and combat input are blocked while the Upgrade menu is active. New Game restores all three prototype values, clears the Upgrade menu/selection/request state, resets Reroll availability and pseudo-random state, clears sequence completion, and starts from Room 1. There is no persistence, stacking, permanent growth, Save, Room 3, or general Stat/Upgrade system. Release Win32/x86 and x64 Rebuilds succeeded with zero warnings and zero errors. The measured M5 executable sizes are `30,208 bytes` for MSVC Release Win32/x86 (`+1,536 bytes` from M4) and `37,376 bytes` for MSVC Release x64 (`+1,024 bytes` from M4). Formula/source validation covered distinct initial and Reroll pairs, a changed Reroll pair, the one-use guard, exact three effect values, menu input isolation, selection completion, and New Game reset branches. Direct execution of the new binaries was blocked by the current Windows Application Control policy. No new source/header, external Library, abstraction/system, or Release setting change was added. Do not implement M6 or any later stage until explicitly requested by the user.

`F00 Flicker Reduction` is complete. All six current `InvalidateRect` calls use `FALSE`, so redraw requests do not request background erasure. `WM_ERASEBKGND` now returns handled with the minimum direct return, preventing a separate Windows client-background clear before paint. The existing `WM_PAINT` flow remains responsible for the full client in one paint: it fills the full client black for letterbox/title coverage and then uses the unchanged `320x180` software framebuffer and single `StretchDIBits` output path. No client-size second backbuffer, Memory DC/BitBlt buffering, Renderer abstraction, graphics API, frame-pacing, `Sleep`, or `timeBeginPeriod` change was added; final frame pacing remains `미정` for later review. Release Win32/x86 and x64 Rebuilds succeeded with zero warnings and zero errors. The measured F00 executable sizes are `30,208 bytes` for MSVC Release Win32/x86 (`+0 bytes` from M5) and `37,376 bytes` for MSVC Release x64 (`+0 bytes` from M5). Runtime x64 validation covered normal launch, Title/New Game, Gameplay input and Audio smoke tests, 40 alternating 4:3/wide resize samples with zero unexpected letterbox or blank/white framebuffer samples, aspect-fit output, minimize/maximize/restore, and normal close. The background erase stage is removed, reducing the repaint opportunity for flicker; no human visual-comparison metric is recorded. No gameplay, AI, Room, Upgrade, project setting, import, source/header, or external Library change was added.

`M5 Upgrade UI Bug Fix` is complete. While the Upgrade menu is active, its input and Window message handling continue but Gameplay simulation and framebuffer rebuild/redraw ticks are paused; QPC timing is refreshed during the pause, and only actual selection, Reroll, or menu-transition changes request repaint. Upgrade Up/Down/Z edge input no longer depends on the Gameplay-only `gameplayInputBlocked` flag. The `RUN CLEAR` 3-bit row read order now matches the bitmap literals without changing their data, order, spacing, or position. Release Win32/x86 and x64 Rebuilds succeeded with zero warnings and zero errors. Executable sizes remained `30,208 bytes` for x86 and `37,376 bytes` for x64 (`+0 bytes` each). Source/formula validation covered modal Update/render isolation, Upgrade selection/confirm/Reroll branches, and all eight correctly oriented `RUN CLEAR` glyphs with unchanged placement; both binaries passed Title/New Game, Gameplay input, Audio, resize, minimize/restore, and close smoke tests. No new source/header, external Library, abstraction/system, project setting, or M6 work was added.

`M6 Minimal Run Start / End Flow` is complete. Player Death now enters a modal `GAME OVER`, while Room 1 -> Room 2 -> Upgrade completion retains the corrected procedural `RUN CLEAR` and enters the same minimal two-choice end flow. Both screens offer `다시 시작` and `타이틀로` with Up/Down and Z; their modal loop continues Window/UI input while pausing Gameplay simulation, AI, Detection, Combat, framebuffer rebuilds, and repeated redraw requests. Restart reuses the existing New Game request/reset path, returning to Room 1 with Player/Enemy/AI/Detection/Combat/Room/Exit/Upgrade/Reroll and Slash/Dash/Execute runtime state reset; held Z and direction input are consumed by the existing Gameplay input block. Title return selects Title Main item zero. Save, Continue/Load, and Settings remain unimplemented. Release Win32/x86 and x64 Rebuilds succeeded with zero warnings and zero errors. The measured M6 executable sizes are `30,720 bytes` for x86 (`+512 bytes` from the latest M5/fix baseline) and `37,376 bytes` for x64 (`+0 bytes`). Runtime x64 validation covered Title -> New Game, Player Death -> GAME OVER, modal stability, GAME OVER restart and Title return, Room 1 -> Room 2 -> Upgrade, Reroll, correctly oriented RUN CLEAR, and RUN CLEAR restart with no combat-input leak. The x86 executable rebuilt successfully, but direct execution was blocked by the current Windows Application Control policy. No new Manager/framework, source/header, external Library, project setting, Save feature, or later-stage work was added.

`M7 Minimal Save / Continue` is complete and completes the first MVP play-flow pass. One `DeadSignal.sav` binary checkpoint uses a 28-byte fixed format containing magic/version, `currentRoom`, the three persistent prototype runtime stat values, and Reroll use state. New Game overwrites it with a Room 1 checkpoint, and the completed Room 1 -> initialized Room 2 transition updates it to Room 2. Continue immediately loads the latest valid checkpoint; Game Start -> Load uses the same single file. Load reuses the existing Room reset path, so Player/Enemy/AI/Detection/Combat/Exit begin at that Room's initial state; no Enemy position, HP, Detection, A* path, cooldown, or other mid-Room state is saved. GAME OVER and RUN CLEAR delete the current Save. Missing files, wrong sizes, and wrong magic/version values are rejected without entering Gameplay and display `저장 없음`. Release Win32/x86 and x64 Rebuilds succeeded with zero warnings and zero errors. The measured M7 executable sizes are `31,744 bytes` for x86 (`+1,024 bytes` from M6) and `38,912 bytes` for x64 (`+1,536 bytes` from M6). Both architectures passed execution smoke tests; x64 runtime validation additionally covered missing/invalid rejection, Room 1 and Room 2 Continue/Load, New Game overwrite, checkpoint-only reset behavior, held menu-input consumption, and GAME OVER/RUN CLEAR deletion. There are no Save Slots, Save selection UI, Cloud Save, backup/recovery/checksum system, new source/header, external Library, or Save Manager/framework. Do not implement a later stage until explicitly requested.

`Q00 MVP Integration QA` is complete. Release Win32/x86 and x64 Rebuilds succeeded with zero warnings and zero errors, and the integrated Title -> New Game -> Room 1 -> Room 2 -> Upgrade/Reroll -> RUN CLEAR -> Restart flow passed on both architectures. x64 additionally passed GAME OVER restart/Title return, Room 1/2 Continue and Load, New Game overwrite, missing/wrong-size/wrong-magic/wrong-version Save rejection, GAME OVER/RUN CLEAR Save deletion, Wall/Dash collision, Detection/FOV state progression, ALERT chase, fixed-grid A* source/path-state checks, Audio, resize/minimize/restore/maximize/close, and a 90-second simultaneous x86/x64 soak; both processes remained responsive and exited normally. BLOCKER=`0`, MAJOR=`0`, and MINOR=`0`; no local bug was reproduced, no code was changed, and there is no unresolved Q00 issue. Actual executable sizes remain `31,744 bytes` for x86 and `38,912 bytes` for x64, and the runtime `DeadSignal.sav` remains `28 bytes`. Against the exact `1,474,560-byte` submission limit, the x86 executable leaves `1,442,816 bytes` and the x64 executable leaves `1,435,648 bytes`; the runtime-generated Save is not part of the packaged executable. This Q00-validated MVP is the next development baseline. Do not implement another feature or optimization until explicitly requested.

`G00 Seeded Room / Run Foundation` is complete. New Game creates one 32-bit `runSeed`, and a small per-Room hash/PRNG derives each Room deterministically from only that seed and `roomIndex`; the prototype Run now contains six Rooms using OPEN, PILLAR, and fixed-layout MAZE types. Current Room setup varies the wall layout variant, Enemy start, Patrol direction/range, and a valid Left/Right Exit, while the existing collision, FOV/LOS, and fixed-grid A* paths use all active Room walls. Upgrade/Reroll is reused after Rooms 2, 4, and 6; the Room 2/4 choice continues to the next Room and the Room 6 choice enters RUN CLEAR. Player HP, selected Upgrade effects, and the one-use Reroll state persist across Room transitions, while Enemy/AI/Detection/A*/Combat runtime state is reset. Save version 2 is a 36-byte Room-start checkpoint containing `runSeed`, `currentRoom`, Player HP, Upgrade stats, and Reroll state; Continue recreates the same Room, and older/incompatible Saves are rejected safely. GAME OVER/RUN CLEAR Save deletion is unchanged. A 12,000-Room deterministic layout/path validation found no invalid Player/Enemy spawn or unreachable Exit, and all five OPEN/PILLAR/MAZE layout variants have connected valid A* grids. Release x86 and x64 Rebuilds succeeded with zero warnings and zero errors. The measured G00 executable sizes are `33,280 bytes` for x86 (`+1,536 bytes` from Q00) and `38,400 bytes` for x64 (`-512 bytes` from Q00). Final rebuilt executables were blocked from direct launch by the current Windows Application Control policy; the pre-final x64 New Game/checkpoint/Continue smoke passed, but final manual playthrough remains environment-blocked. TRAP, MIXED, procedural maze generation, and new Enemy types are not implemented. Do not implement the next stage until explicitly requested.

`G01 Larger World Prototype` is complete. The viewport/framebuffer remains `320x180` at 16:9, while prototype Room gameplay now uses a `640x360` World. Player, Enemy, Walls, Exit, Patrol, FOV/LOS/Detection, Combat, and A* remain in World Space; rendering alone subtracts one directly calculated Camera offset. The Camera follows the Player without smoothing using the viewport half-size and clamps to World edges at X=`0..320` and Y=`0..180`. OPEN remains the lowest-density type, while PILLAR and MAZE keep G00-scale obstacle dimensions and maintain the larger World's density by increasing structure count and spreading placement; increasing World size must not scale individual obstacles. MAZE complexity is intentionally unchanged and will be reconsidered after the G part. The fixed navigation cell size remains 8, with the grid minimally expanded to `80x44` (`3,520` nodes). All five layout grids are connected, and a repeated 12,000-Room seed validation found zero invalid Player/Enemy spawns while preserving Room type/layout/Exit/Enemy determinism. Save v2 remains 36 bytes and stores no Camera state; Continue recreates the same Room and immediately derives Camera position from the reset Player. Final x64 runtime validation covered New Game, direct Camera tracking, all four World-edge clamps, Camera-relative Walls/FOV/Detection, Continue with an unchanged checkpoint, and normal close; x86 passed launch/close smoke. Release x86 and x64 Rebuilds succeeded with zero warnings and zero errors. The measured G01 executable sizes are `33,792 bytes` for x86 (`+512 bytes` from G00) and `38,400 bytes` for x64 (`+0 bytes` from G00). There is still one Enemy, and no smooth Camera, zoom, minimap, new Room type, or new Renderer/system was added. Final Room size is expected to vary with Enemy count, with more Enemies potentially requiring more area; the exact Enemy Count-to-Room Size rule remains undecided, and the current `640x360` is a fixed prototype with no automatic scaling. Do not implement G02 until explicitly requested.

`G02 Multiple Enemies Prototype` is complete. Every Room now deterministically creates three instances of the existing prototype Enemy in the fixed `640x360` World using one small `EnemyRuntime` fixed array and fixed valid spawn candidates; there is no new Enemy type, EnemyManager, Entity/ECS system, dynamic allocation, or generic AI framework. Each Enemy independently owns HP/alive state, position/Facing/Patrol, CALM/SUSPICION/ALERT Detection and FOV, last-seen/ALERT/Search/Scan state, A* path state, attack cooldown, and hit/Execute feedback. A* parent/score/state scratch memory is safely reused sequentially, while each Enemy has a separate fixed path result buffer so concurrent searches cannot overwrite another Enemy's retained route. Enemy ALERT is not shared. Slash applies one hit to every overlapping Enemy as a G02 prototype rule whose final simultaneous-hit limit remains undecided; Execute removes only the nearest valid overlapping CALM Enemy; Dash still causes no Damage. An Enemy death stops only that Enemy, successful kills retain the Slash/Dash cooldown reset rule, and the Exit unlocks only after all three Enemies are dead. Room entry resets all three Enemy runtimes while Player HP and Run Upgrade/Reroll persist. Save v2 remains exactly `36 bytes`, stores no Enemy runtime, and Continue regenerates the same three spawn/Patrol results from the same seed and Room index. A repeated 12,000-Room validation found zero invalid, overlapping, navigation-invalid, or unreachable Enemy spawns and zero determinism mismatches; all five existing OPEN/PILLAR/MAZE navigation grids remained fully connected. Release x86 and x64 Rebuilds succeeded with zero warnings and zero errors. Runtime validation covered x64 New Game/checkpoint/Continue, Camera-relative Enemy/FOV rendering, independent ALERT presentation, GAME OVER regression, a responsive 90-second movement soak without crash/hang, and x86 launch/close smoke; the full six-Room manual run was not repeated in this environment. The measured G02 executable sizes are `34,304 bytes` for x86 (`+512 bytes` from G01) and `41,984 bytes` for x64 (`+3,584 bytes` from G01). World size remains fixed at `640x360`; Enemy Count-to-Room Size coupling remains deferred. Do not implement G03 until explicitly requested by the user.

`G03 Room Scale + Gameplay HUD/Menu + Player Health Bar` is complete. The five Room Size stages are Size 1=`160x90`, Size 2=`320x180`, Size 3=`480x270`, Size 4=`640x360`, and Size 5=`800x450`; the current six-Room Run uses Size 2 for Rooms 1-2, Size 3 for Rooms 3-4, and Size 4 for Rooms 5-6, while Size 1/5 remain supported but unused. The viewport remains `320x180`. Camera ranges clamp safely against the current World with no negative range for Size 1, and A* uses only the current Room's columns/rows inside Size 5 fixed-capacity scratch/path storage. OPEN/PILLAR/MAZE retain fixed obstacle dimensions while structure counts and placement ranges increase with Room area; Maze quality was not redesigned. Enemy Count remains exactly three and its final coupling to Room Size remains undecided. A screen-space Gameplay HUD displays Enemy Remaining/Total, two-digit Room Number.Type, persistent Run Kill Count, and a runtime three-line Menu Icon. A fixed `12x2` runtime Player Health Bar follows the Player in World Space with Camera offset and fills directly from current HP/10; it has no number, animation, delay, gradient, widget, or system abstraction. ESC or Menu Icon click opens a modal Gameplay Menu with Resume, Settings placeholder, and Save & Title; Up/Down/Z, ESC close, and direct menu-item mouse clicks are handled without a generic Mouse/Menu/UI system. Its modal loop pauses Gameplay, AI, Detection, Combat, Player movement, and Camera updates. Save & Title writes the current persistent Run state but retains Room-start checkpoint semantics for Enemy runtime. Save version 3 is exactly `40 bytes` and adds Run Kill Count; older 36-byte version-2 Saves are rejected by the existing exact-size/version validation. Player HP and Run Kill Count persist through Room transitions and Continue, while New Game/Restart restores HP 10 and Kill Count zero; GAME OVER/RUN CLEAR deletion remains unchanged. Formula validation covered Room stage mapping, all five Camera ranges, and HP fill widths. Every Size 1-5 OPEN/PILLAR/MAZE layout variant had zero out-of-bounds Walls, Player/Exit overlaps, or disconnected navigation cells, and repeated validation of 12,000 current-Run Rooms found zero invalid spawns, Enemy overlaps, unreachable spawns, or determinism mismatches. Release x86 and x64 Rebuilds succeeded with zero warnings and zero errors. Direct execution of the final Release binaries was blocked by the current Windows Application Control policy, so final HUD/Menu visual and six-Room manual runtime validation remain environment-blocked. The measured G03 executable sizes are `39,424 bytes` for x86 (`+5,120 bytes` from G02) and `46,080 bytes` for x64 (`+4,096 bytes` from G02). No Trap, Mixed Room, new Enemy type, automatic Enemy count, actual Settings, new source/header, external Library, Manager, or size optimization was added. Do not implement G04 until explicitly requested by the user.

`G04 Trap Room Prototype` is complete. Seeded Room Type selection now includes OPEN, PILLAR, MAZE, and TRAP, and the HUD renders TRAP as `NN.Trap`. A TRAP Room is an open, wall-free combat space containing one fixed-array periodic floor-hazard type; hazards are fixed `24x14` logical pixels and use area-proportional Size-stage counts `1/3/7/12/19`, so the current Size 2/3/4 Run Rooms use `3/7/12` hazards without scaling their dimensions. Each hazard starts in deterministic OFF phase with a seed-derived offset, cycles at OFF `1.25 seconds` and ACTIVE `0.75 seconds` (`prototype initial values`), and renders a dark outline/pattern when OFF and a bright pattern when ACTIVE. ACTIVE overlap damages the Player by 1 with a shared approximately `0.5-second` prototype re-hit cooldown; Dash grants no immunity, the Health Bar reflects HP immediately, and Trap damage can enter the existing GAME OVER flow. Enemies take no Trap damage and Trap state does not affect Detection or AI. Traps remain passable to Player/Enemy movement and are deliberately absent from A* obstruction checks. Player, all three Enemy starts, and Exit reject Trap overlap; the scattered layout preserves a safe Player-to-Exit route. Gameplay Menu, Upgrade, GAME OVER, and RUN CLEAR modal loops stop Trap elapsed time, with the existing QPC reset preventing Resume time jumps. Trap phase is runtime-only, resets at Room entry/Continue, and is not checkpointed. Because adding TRAP changes seeded Room Type interpretation, Save version is now 4 while the structure remains exactly `40 bytes`; version-3 and other incompatible Saves are rejected. Validation across seeds `1..100,000` covered `150,260` generated TRAP Rooms with zero invalid/overlapping starts, unreachable starts, determinism mismatches, Exit-Trap overlaps, or requested-count mismatches; seed `1`, Room `1` is a verified TRAP case. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors, and both architectures passed launch/title/close smoke. The measured G04 executable sizes are `40,960 bytes` for x86 (`+1,536 bytes` from G03) and `46,592 bytes` for x64 (`+512 bytes` from G03). Exact Trap balance and additional Trap types remain undecided. No Mixed Room, new Enemy type, Maze redesign, Enemy-count scaling, Trap avoidance/damage for Enemies, new file/library, generic Manager/system, or size optimization was added. Do not implement G05 until explicitly requested by the user.

`G05 Mixed Room Prototype` is complete. Seeded Room Type selection now includes OPEN, PILLAR, MAZE, TRAP, and MIXED, and the HUD renders MIXED as `NN.Mixed`. MIXED combines fixed `16x24` Wall/Pillar obstacles, the unchanged periodic `24x14` passable Trap, and the existing three independent Enemies without adding a new generator or system. Two deterministic mirrored fixed-candidate layout variants use Size 1-5 obstacle counts `1/2/4/7/12` and Trap counts `1/2/4/7/11`; the current Size 2/3/4 Run therefore uses `2/4/7` obstacles and `2/4/7` Traps. Individual obstacle and Trap dimensions remain fixed as Room Size grows, while MIXED combines lower per-element density than the corresponding standalone PILLAR/TRAP Rooms; the Size 2 Trap ratio is the prototype integer-rounding exception. Walls remain blocked for Player/Enemy collision and A*, and occlude FOV/LOS; Traps remain passable, do not occlude FOV/LOS, damage only the Player under the unchanged G04 timing/damage rules, and keep runtime phase out of Save data. Enemy count remains exactly three, and Enemy Count-to-Room Size coupling remains unimplemented. Adding MIXED changes seeded Room Type interpretation, so Save version is now 5 while `SaveCheckpoint` remains exactly `40 bytes`; all older/incompatible versions are rejected safely, and Continue regenerates MIXED layout, traps, Enemy spawn/Patrol, and Exit from only the saved seed and Room index. Validation covered seeds `1..100,000` across all six current Run Rooms plus Size 1/5 seed samples, totaling `640,000` Rooms, with zero invalid spawns, overlaps, unreachable layouts/A* paths, determinism mismatches, Wall-Trap overlaps, blocked Exits, requested-count mismatches, Wall A*/LOS failures, or Trap A* blocking failures. OPEN/PILLAR/MAZE/TRAP generation branches were not redesigned. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. The measured G05 executable sizes are `41,472 bytes` for x86 (`+512 bytes` from G04) and `47,616 bytes` for x64 (`+1,024 bytes` from G04). No new Enemy type, Enemy-count change/coupling, Maze improvement, Trap type, Enemy Trap damage, shared ALERT, Upgrade/Reward, new file/library, generic Room generator/Manager, or size optimization was added. Do not implement the next stage until explicitly requested by the user.

`G06 Watcher Enemy Prototype` is complete. Enemy Role now has the minimal `PATROLLER` and `WATCHER` values stored per `EnemyRuntime`; the existing Enemy behavior remains the PATROLLER baseline, and every current Room deterministically contains Enemy 0 as one WATCHER plus Enemies 1-2 as two PATROLLERs. The WATCHER starts at the existing valid deterministic Enemy candidate and selects one seed-derived cardinal surveillance Facing, trying the four directions in deterministic order until approximately `24 logical pixels` ahead is inside the Room and clear of a Wall. CALM WATCHER behavior is stationary surveillance at its starting position and Facing. SUSPICION remains stationary while reusing the existing smooth Player/last-seen Facing, FOV/LOS, Detection fill/decay, and red FOV presentation. ALERT reuses the complete existing chase, A*, Search/Scan, contact Attack, HP, Damage, Death, Slash, and Execute paths without Watcher-specific stats. After ALERT ends and Detection decays to CALM, the WATCHER reuses A* to return to its exact starting position and restores its original surveillance Facing, then remains stationary. PATROLLER movement and all existing values remain unchanged, and each Enemy continues to own independent Detection/ALERT/AI/A*/Combat state with no Alert sharing. A small runtime-generated bright eye stripe distinguishes the WATCHER while retaining the existing silhouette and no Asset. The prototype WATCHER keeps the common Range `70` and FOV `55 degrees`; final Watcher-specific FOV/stats and final Enemy composition rules remain undecided. OPEN/PILLAR/MAZE/TRAP/MIXED layouts, densities, Traps, and Enemy count remain unchanged. Save remains version 5 and exactly `40 bytes`; role/runtime AI is not saved, while Continue regenerates the same WATCHER spawn/Facing and fixed role composition from seed and Room index. Validation covered seeds `1..100,000` across all six current Run Rooms plus Size 1/5 samples, totaling `640,000` Rooms, with zero invalid spawns, overlaps, unreachable layouts/A* paths, determinism mismatches, invalid cardinal Facings, or immediately Wall-blocked Watcher Facings. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors, and both executables passed process-launch smoke. The measured G06 executable sizes are `41,984 bytes` for x86 (`+512 bytes` from G05) and `48,128 bytes` for x64 (`+512 bytes` from G05). No Hunter, Listener, Spinner, Open-specific pressure role, shared Alert, Enemy-count change/coupling, Watcher-specific Combat/FOV balance, Maze/Room/Trap change, new file/library, generic Enemy system/hierarchy, or size optimization was added. Do not implement the next stage until explicitly requested by the user.

`G07 Hunter Enemy Prototype` is complete. Enemy Roles are now `PATROLLER`, `WATCHER`, and `HUNTER`, and every current Room deterministically contains Enemy 0 as WATCHER, Enemy 1 as PATROLLER, and Enemy 2 as HUNTER. HUNTER reuses the PATROLLER baseline for CALM Patrol, SUSPICION smooth Facing/last-seen investigation, Detection/FOV/LOS, Patrol-route return, movement speed, A*, Search/Scan, Combat, HP, and Attack. Only ALERT persistence differs: HUNTER uses a prototype reacquire range of `70 logical pixels` instead of the unchanged common `42`, and an approximately `15-second` failed-search timeout instead of the unchanged common approximately `10 seconds`; chase speed remains the common `58 logical pixels per second`, Vision Range remains `70`, and Scan remains `2 seconds`. HUNTER returns to the existing Patrol area without teleporting after ALERT/SUSPICION fully decays to CALM. WATCHER stationary surveillance/return behavior and PATROLLER behavior remain unchanged, and all three Enemies retain independent Detection/ALERT/A*/Search/Attack/death state with no Alert sharing. A small runtime-generated bright vertical body mark distinguishes HUNTER while reusing the existing silhouette and no Asset; HUD role text was not added. OPEN/PILLAR/MAZE/TRAP/MIXED layouts, density, Trap behavior, Enemy count, Camera, HUD/Menu, Upgrade, and Health Bar remain unchanged. Save remains version 5 and exactly `40 bytes`; role/runtime AI is not saved, while Continue regenerates the same fixed roles and deterministic Watcher/Patroller/Hunter spawn, Facing, and Patrol data. Validation covered seeds `1..100,000` across all six current Run Rooms plus Size 1/5 samples, totaling `640,000` Rooms, with zero invalid spawns, overlaps, unreachable layouts/A* paths, determinism mismatches, Hunter Search-path failures, or role-rule failures. Formula/source validation confirmed HUNTER `70/15`, common `42/10`, shared `58` chase speed and `2`-second Scan, independent state ownership, unchanged Watcher/Patroller branches, common Combat, and common Exit unlock. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors, and both executables passed process-launch smoke. The measured G07 executable sizes are `42,496 bytes` for x86 (`+512 bytes` from G06) and `48,128 bytes` for x64 (`+0 bytes` from G06). Final Hunter balance and final Room Enemy composition remain undecided. No Listener, Spinner, Open-specific pressure role, shared Alert, Hunter speed/Combat-stat change, Enemy-count change/coupling, Maze/Room/Trap change, new Upgrade, file/library, generic AI framework, or size optimization was added. Do not implement the next stage until explicitly requested by the user.

`G08 Listener Enemy Prototype` is complete. Enemy Roles are now `PATROLLER`, `WATCHER`, `HUNTER`, and `LISTENER`; PATROLLER remains the unchanged baseline Role but the current deterministic three-Enemy composition is Enemy 0 WATCHER, Enemy 1 HUNTER, and Enemy 2 LISTENER. LISTENER reuses PATROLLER CALM Patrol, Vision/Detection, Patrol return, Combat, and the common ALERT `42 logical pixel` reacquire / approximately `10-second` failed-search / `58 logical pixels per second` chase policy. Its added hearing branch reacts only when Player coordinates actually change: normal movement uses a prototype `80 logical pixel` range and active Dash movement uses `120`; stationary input blocked by a Wall produces no noise. Hearing ignores Wall/Pillar occlusion, works independently of Facing/FOV, enters only hearing SUSPICION with the existing `?`, stores the current Player position in the reused investigation fields, smooth-turns, and follows an Enemy-owned A* path toward a valid adjacent investigation point. The target path is rebuilt only after at least one `8 logical pixel` navigation-cell displacement; hearing does not fill the red visual Detection progress and never enters ALERT directly. Once noise stops at the investigation point, LISTENER walks back to its original Patrol area without teleporting. Slash, Execute, and Trap produce no hearing event; Trap remains A*-passable and causes no Enemy damage. WATCHER behavior and HUNTER `70` reacquire / approximately `15-second` failed-search behavior remain unchanged, and Alert/Hearing is not shared. A small runtime-generated paired head mark distinguishes LISTENER without an Asset or HUD Role text. OPEN/PILLAR/MAZE/TRAP/MIXED generation, density, Enemy count, and other existing gameplay systems remain unchanged. Save remains version 5 and exactly `40 bytes`; hearing/AI runtime is not saved, while Continue deterministically regenerates the same Watcher/Hunter/Listener spawn, Facing, and Patrol data. Revalidation of seeds `1..100,000` across all six current Run Rooms totaled `600,000` Rooms with `invalid spawn=0`, `overlap=0`, `unreachable=0`, and `determinism mismatch=0`; hearing boundary/source checks also passed, and a 90-second x64 movement/stop/Dash soak remained responsive and closed normally. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors, and both final executables passed launch/close smoke. The measured G08 executable sizes are `44,032 bytes` for x86 (`+1,536 bytes` from G07) and `50,176 bytes` for x64 (`+2,048 bytes` from G07). Exact Listener balance and final Room Enemy composition remain undecided. No Spinner, Open-specific pressure Role, generic Noise/Sound propagation system, Sound occlusion, shared Alert/Hearing, Enemy-count change/coupling, Room/Maze/Trap change, new Upgrade, file/library, generic AI framework, or size optimization was added. Do not implement the next stage until explicitly requested by the user.

`G09 Spinner Enemy Prototype` is complete. Enemy Roles are now `PATROLLER`, `WATCHER`, `HUNTER`, `LISTENER`, and `SPINNER`; PATROLLER and HUNTER remain unchanged but the current deterministic three-Enemy composition is Enemy 0 WATCHER, Enemy 1 LISTENER, and Enemy 2 SPINNER. SPINNER is stationary at its Home Position during CALM and advances its existing Facing continuously at `1.04719755 radians per second`, producing one clockwise `360-degree` surveillance rotation in approximately `6.0 seconds` while reusing the common Range `70`, FOV `55 degrees`, Detection, Wall/Pillar LOS clipping, and Trap-nonblocking rules. Rotation is applied immediately before visual Detection, so Detection and the rendered FOV use the same updated Facing. Visual SUSPICION pauses automatic rotation, keeps SPINNER stationary, and reuses smooth Player tracking plus the common Detection hold/decay. ALERT reuses the common `58 logical pixels per second` chase, `42 logical pixel` reacquire, approximately `10-second` failed-search, A*, and `2-second` Scan policy rather than HUNTER `70/15`. After Detection fully decays, SPINNER walks by A* to its exact deterministic Home Position without teleporting and resumes rotation from its arrival Facing; WATCHER alone still restores its original surveillance Facing. Gameplay Menu, Upgrade, GAME OVER, and RUN CLEAR stop the simulation update and reset QPC timing, so rotation pauses without a Resume angle jump. SPINNER has no Hearing behavior, Combat stat, or HUD Role text; a small runtime-generated horizontal body mark distinguishes it without an Asset. Its initial cardinal Facing is selected deterministically from the Room seed after existing Room generation, without changing Room layout, spawn, Patrol, Trap, or density rules. Save remains version 5 and exactly `40 bytes`; rotation phase and other Enemy runtime state are not saved, while Continue recreates the same spawn, Home, initial Facing, and CALM rotation start. Revalidation of seeds `1..100,000` across all six current Run Rooms totaled `600,000` Rooms with `invalid spawn=0`, `overlap=0`, `unreachable=0`, `determinism mismatch=0`, and `invalid initial facing=0`. Formula/source validation confirmed the six-second rotation, shared Facing for Detection/rendering, stationary CALM/SUSPICION, exact-Home return, common `42/10` ALERT policy, unchanged WATCHER/LISTENER/HUNTER/PATROLLER branches, Wall LOS clipping, Trap non-occlusion, and modal QPC pause path. A 90-second x64 movement/stop/Dash soak with seven Gameplay Menu pause/resume cycles remained responsive and closed normally; a separate isolated New Game checkpoint measured `40 bytes`. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. The measured G09 executable sizes are `44,032 bytes` for x86 (`+0 bytes` from G08) and `50,688 bytes` for x64 (`+512 bytes` from G08). Exact Spinner balance and final Room Enemy composition remain undecided. No Open-specific pressure Role, Enemy-count change/randomization/coupling, shared Alert/Hearing, Spinner Hearing/Combat stat, Room/Maze/Trap change, new Upgrade, file/library, generic rotation/AI framework, or size optimization was added. Do not implement the next stage until explicitly requested by the user.

`G10 Open Room Pressure Enemy Prototype` is complete. Enemy Roles now additionally include `PRESSURE`, while the existing PATROLLER/WATCHER/HUNTER/LISTENER/SPINNER code and the current normal composition of Enemy 0 WATCHER, Enemy 1 LISTENER, and Enemy 2 SPINNER remain unchanged. Exactly one PRESSURE Enemy is activated only in OPEN Rooms, producing three normal Enemies plus one pressure Entity there; PILLAR, MAZE, TRAP, and MIXED create none. PRESSURE uses a separate `EnemyRuntime` instance so all normal Enemy loops remain fixed at three: it cannot receive Slash or Execute damage, has no HP/death path, does not contribute to Exit unlock, HUD Remaining/Total, or Run Kill Count, and never renders FOV, `?`, or `!`. Its runtime-generated silhouette is `10x12 logical pixels`, visibly wider than a normal Enemy, while its collision remains the existing A*-safe `8x12` footprint; this prototype mismatch avoids changing the shared navigation-cell validity rules. PRESSURE continuously targets the Player at exactly `29 logical pixels per second`, half the common `58` ALERT speed. It uses direct movement when center LOS is clear and reuses its own retained path slot plus the existing sequential A* scratch when an OPEN obstacle blocks the route; path targets update after one `8-pixel` navigation-cell displacement and choose a valid Player-adjacent point with a clear final leg. Contact attacks require no Detection/ALERT and reuse the common Player damage, `1.0-second` attack cooldown, contact tolerance, feedback, and GAME OVER path. Modal simulation pauses stop PRESSURE and existing QPC resets prevent Resume delta jumps. OPEN spawn deterministically reuses an unused fixed Enemy candidate after all existing Room/Enemy random interpretation, rejects visual-footprint Wall/Player/Exit/normal-Enemy overlap and world bounds, requires a valid connected `8x12` navigation cell, and keeps at least `80 logical pixels` from the Player start. Validation of seeds `1..100,000` across all six current Rooms covered `600,000` Rooms including `119,954` OPEN Rooms with `invalid Pressure spawn=0`, `overlap=0`, `bounds error=0`, `unreachable=0`, `non-OPEN Pressure=0`, and `determinism mismatch=0`. Save remains version 5 and exactly `40 bytes`; PRESSURE runtime position/path/cooldown is not saved, and an isolated OPEN checkpoint plus Continue regenerated the Room-start state. A 90-second OPEN-selected x64 movement/Dash/Menu soak remained responsive through GAME OVER and closed normally; it does not claim a full 90 seconds of surviving live combat. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. The measured G10 executable sizes are `47,616 bytes` for x86 (`+3,584 bytes` from G09) and `54,272 bytes` for x64 (`+3,584 bytes` from G09). Exact final Pressure balance remains undecided. No multiple Pressure Enemies, Pressure acceleration/FOV/Detection/Hearing, killability, generic invulnerability/enemy framework, Enemy-count-to-Room-size coupling, Room/Maze/Trap change, new Upgrade, file/library, or size optimization was added. Do not implement the next stage until explicitly requested by the user.

`G11 G-Part Integration QA / Playtest Audit` is complete with no Gameplay or balance change and no code fix required. G01-G10 are now the current integrated gameplay-prototype baseline. The audit found `BLOCKER=0`, `MAJOR=0`, and `MINOR=0`; unautomated feel/balance/visual judgments are classified only as PLAYTEST items, not bugs. Revalidation of seeds `1..100,000` across all six current Rooms covered `600,000` Rooms with all five Room Types represented and produced `invalid spawn=0`, `Entity overlap=0`, `Wall/Trap overlap=0`, `Exit blocked=0`, `unreachable=0`, `invalid facing=0`, and `determinism mismatch=0`. Additional checks produced zero Wall A*-blocking, Trap A*-passability, Wall LOS-occlusion, Trap LOS-nonocclusion, or OPEN-only PRESSURE placement failures. Current Room mapping remains Rooms 1-2 Size 2=`320x180`, Rooms 3-4 Size 3=`480x270`, and Rooms 5-6 Size 4=`640x360`; supported Size 1=`160x90` and Size 5=`800x450` remain unused, Camera maxima for Size 1-5 are `(0,0)`, `(0,0)`, `(160,90)`, `(320,180)`, and `(480,270)`, and fixed obstacle/Trap dimensions remain unscaled while counts grow. Current Size 2/3/4 data counts are OPEN Walls `1/3/5`, PILLAR Walls `4/6/12`, MAZE Walls `1-3/4-8/10-12`, TRAP hazards `3/7/12`, and MIXED Walls+Traps `2+2/4+4/7+7`. Source/runtime audit confirmed the independent normal-Enemy runtime/path ownership, sequential shared A* scratch, inactive PATROLLER/HUNTER Role branches, active WATCHER/LISTENER/SPINNER composition, OPEN-only separate PRESSURE runtime, multi-hit Slash, nearest valid CALM Execute, per-Enemy HP/attack cooldown, normal-three-only Exit/HUD/Kill Count, Trap-only Player damage, modal pause paths, checkpoint state, and GAME OVER/RUN CLEAR save deletion branches. Save validation passed for version 5 and exactly `40 bytes`, including valid checkpoint/Continue regeneration and rejection of incompatible version, magic, seed, Room, HP, and byte size. An isolated x64 integration soak ran `91.0 seconds`: OPEN/PRESSURE `45.5 seconds` plus MIXED/Trap `45.5 seconds`, each with `58` movement/Dash/Slash iterations and `5` Gameplay Menu pause/resume cycles; both remained responsive without crash, hang, or observed path corruption, Save & Title -> Continue remained responsive with a `40-byte` checkpoint, and x86 launch/title/close smoke passed. A complete six-Room clear/failure playthrough and pixel-level UI flicker/glyph-position observation still require manual playtesting. Required PLAYTEST review remains: early Size 2 suitability, Size 3/4 feel, Size 5 necessity, and future Enemy-count-to-Size mapping; OPEN emptiness, PILLAR/TRAP density, MAZE identity, and MIXED complexity; whether three Enemies and the Room-specific composition are readable; whether WATCHER/HUNTER/LISTENER/SPINNER/PRESSURE create distinct play; and Slash multi-hit, Execute, simultaneous attacks, Player HP 10, and PRESSURE contact pressure. Release Rebuilds succeeded with zero warnings and zero errors. Actual executable sizes remain `47,616 bytes` for x86 and `54,272 bytes` for x64; remaining executable budgets against the exact `1,474,560-byte` limit are `1,426,944 bytes` and `1,420,288 bytes`, respectively. G-Part polish/balance must wait for the user's manual evaluation, and no next work may be implemented until explicitly instructed.

`H00 Seeded Run Composition Generation` is complete. The Run remains six Rooms with Upgrade after Rooms 2, 4, and 6 followed by RUN CLEAR. The prototype normal-Enemy progression is now `2/2/3/3/4/4`; Room setup first derives this count, then derives Room Size from the count rather than directly from Room index using `1-2 -> Size 2`, `3 -> Size 3`, `4 -> Size 4`, and `5+ -> Size 5`. Size 1 remains supported but unused. The normal Role pool is PATROLLER/WATCHER/HUNTER/LISTENER/SPINNER. A Room-specific RNG derived from the existing seed and Room index performs one fixed five-entry shuffle, and the first current-Enemy-count entries are selected, so every Room composition is deterministic and contains no duplicate Role. Existing Role behavior and balance values are unchanged. Fixed storage now supports exactly four normal `EnemyRuntime` entries and four independent retained A* paths; the existing separate PRESSURE runtime uses a fifth retained path slot. PRESSURE remains an OPEN-only extra Entity and is excluded from normal Enemy Count, Room Size, HUD Total/Remaining, Exit unlock, and Run Kill Count. All normal-Enemy loops, HUD, spawn, AI, FOV, Combat, death, and Exit logic now use the current Room's count. Room Type interpretation and Size-specific Room/Trap density data remain unchanged. Save remains version 5 and exactly `40 bytes`; Enemy Count, Size, Role order, spawn/patrol/home/Facing, layout, Pressure, and Exit are regenerated from seed and Room index rather than stored. Validation across seeds `1..100,000` and all six Rooms covered `600,000` Rooms with `incorrect enemy count=0`, `incorrect Room Size=0`, `duplicate Role=0`, `invalid spawn=0`, `Entity overlap=0`, `unreachable=0`, `invalid facing/home/patrol=0`, `Wall/Trap overlap=0`, `Exit blocked=0`, `Pressure placement failure=0`, and `determinism mismatch=0`. Role generation counts were PATROLLER `360,268`, WATCHER `359,619`, HUNTER `360,324`, LISTENER `360,160`, and SPINNER `359,629`, confirming every Role is selectable without evaluating balance. An isolated x64 soak ran `90.2 seconds` across a 2-Enemy OPEN+PRESSURE Room, a 3-Enemy TRAP Room, and a 4-Enemy MIXED Room; it covered all five normal Roles, performed two Save & Title -> Continue cycles per case, remained responsive without crash/hang/observed path corruption, retained a `40-byte` checkpoint, and passed x86 launch/title/close smoke. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. Actual executable sizes are `48,128 bytes` for x86 (`+512 bytes` from G11) and `56,320 bytes` for x64 (`+2,048 bytes` from G11). Exact Enemy Count/Size mapping, Role weights/composition, and balance remain prototype values for later review. The final `1,474,560-byte` limit applies to the complete submission package including `DeadSignal.exe`, required runtime DLLs, and every other required deployment file; package-total validation remains required, and no runtime copying, `/MT` comparison, or size optimization was performed in H00. Do not implement the next stage until explicitly requested by the user.

`H01 Run Upgrade System Completion` is complete. The existing modal flow remains two deterministic choices after Rooms 2, 4, and 6, one confirmed choice, and one Reroll for the entire Run, with the Room-6 choice followed by RUN CLEAR. The six-entry pool is divided into AUGMENT MOVE+/SLASH+/DASH+, each selectable at most twice per Run, and FUNCTIONAL SILENT DASH/EXECUTE REACH/FIELD MEDIC, each selectable at most once; a maxed entry is excluded from subsequent offers. AUGMENT stacks are recalculated additively from base values: MOVE is `60/66/72 logical pixels per second`, Slash cooldown is `0.5/0.4/0.3 seconds`, and Dash cooldown is `1.0/0.8/0.6 seconds` at stacks `0/1/2`. SILENT DASH suppresses only movement noise during an active Dash for LISTENER while ordinary movement Hearing remains `80`; visual Detection and every other Role remain unchanged. EXECUTE REACH changes the existing prototype reach from `4` to `8 logical pixels` while preserving CALM-only, nearest-one, Pressure-excluded Execute. FIELD MEDIC restores exactly 1 HP only after a successful Execute kill and clamps naturally at the unchanged maximum 10; Slash kills do not heal and the existing Health Bar reads the updated HP immediately. Upgrade UI identifies AUGMENT/FUNCTIONAL and displays the current `n/2` or `n/1` stack beside both offers, while preserving the existing Reroll state display and modal input/rendering. Offer generation builds the currently available fixed six-ID list, deterministically shuffles it from `runSeed + current Room/Upgrade occurrence + initial-or-Reroll salt`, selects two distinct entries, and guarantees a changed pair on Reroll when more than two candidates exist. Upgrade stacks/flags and Reroll state persist through Room transitions and reset only on New Game/Restart. Save version is now 6 and `SaveCheckpoint` is exactly `32 bytes`; it stores three AUGMENT bytes, one FUNCTIONAL bit field, and Reroll directly, recalculates runtime stats on Continue, retains the Room-start checkpoint policy, and rejects previous v5/40-byte or invalid stack/flag data. Offer stress over seeds `1..100,000`, all three Upgrade Rooms, and eight availability states produced `duplicate option=0`, `maxed option offered=0`, `determinism mismatch=0`, `invalid Upgrade ID=0`, and `unchanged eligible Reroll pair=0`; all six IDs appeared. Formula checks passed for every AUGMENT stack and all FUNCTIONAL owned/unowned branches, Save validation passed, and the H00 `600,000`-Room count/size/Role/spawn regression had zero failures. An isolated v6 x64 Continue run used a TRAP Room containing WATCHER/SPINNER/LISTENER/HUNTER with saved MOVE 1-stack plus SILENT DASH, remained responsive for `30.4 seconds` through movement/Dash/Execute input and two Save & Title -> Continue cycles, retained a `32-byte` checkpoint, and passed x86 launch/title/close smoke. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. Actual executable sizes are `49,152 bytes` for x86 (`+1,024 bytes` from H00) and `57,344 bytes` for x64 (`+1,024 bytes` from H00). All Upgrade names, values, and effects remain prototype balance for later review. The final `1,474,560-byte` limit applies to the complete submission package including required runtime DLLs; no `/MD` versus `/MT` comparison, runtime packaging, or size optimization was performed in H01. Do not implement the next stage until explicitly requested by the user.

`H02 Character Selection & Runtime Profiles` is complete. Title `Game Start -> New Game` now opens a six-entry Character Select containing BASIC/기본형, MOBILITY/기동형, PIERCER/찌르기형, HEAVY/중량형, RAPID/연격형, and Back; Up/Down, Z, ESC, and the existing Gameplay input-release block are reused without a new UI system. H02 temporarily makes all five Characters selectable solely for system validation; BASIC will remain the default-access Character and actual Unlock gating/currency belongs to H03 or later. One fixed `CharacterId` and five fixed profiles apply Character base values before H01 Run Upgrades. Prototype Max HP is `10/9/8/12/9`, Move scale is `100/110/105/85/105%`, Slash cooldown scale is `100/90/110/125/75%`, Slash simultaneous hit cap is `3/2/1/4/2`, and Dash distance scale is `100/110/100/90/100%` for BASIC/MOBILITY/PIERCER/HEAVY/RAPID. BASIC retains the existing `8x8`, damage-1 Slash; MOBILITY and RAPID use a `7x8`, damage-1 Slash; HEAVY uses a `7x8`, damage-2 Slash; and PIERCER uses a deterministic rotated straight `12x4 logical pixel` Slash. Execute remains common, CALM-only, nearest-one, and Pressure-excluded. MOVE+/SLASH+/DASH+ multiply each Character's base profile rather than replacing it with BASIC values, and SILENT DASH/EXECUTE REACH/FIELD MEDIC remain common; FIELD MEDIC and the Health Bar now use the active Character's Max HP. Room transition preserves Character/HP/Upgrades, Restart starts a new Run with the same Character, while Title New Game returns to Character Select. Minimal runtime Player pattern differences identify all five profiles without changing the common `8x12` visual/collision bounds. Character-specific Dash capacity caps are intended to differ, but their exact values and the capacity/charge system remain undecided for H03 or later. Save version is now 7 and remains exactly `32 bytes`; it stores only Character ID in addition to H01 state, reconstructs profile and resulting Upgrade stats on Continue, validates HP against that Character's Max HP, and safely rejects previous versions. Profile/formula, PIERCER geometry, Field Medic, and Save round-trip validation passed. Comparing all five Characters across seeds `1..100,000` and all six Rooms produced `Room generation determinism mismatch=0`; an x64 automated Character Select run for each Character confirmed saved IDs, initial HP, and input-to-Gameplay transition with zero failures. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. Actual executable sizes are `50,688 bytes` for x86 (`+1,536 bytes` from H01) and `58,880 bytes` for x64 (`+1,536 bytes` from H01). All Character profile values, names, and final balance remain prototype values. The final `1,474,560-byte` limit applies to the complete submission package including required runtime DLLs; no runtime packaging, `/MD` versus `/MT` comparison, or size optimization was performed in H02. Do not implement the next stage until explicitly requested by the user.

`H03 Meta Progression + Character Unlock + Dash Capacity` is complete. `DeadSignal.meta` is a separate permanent version-1 fixed `16-byte` binary profile containing magic/version, nonnegative Global Coin, the five-bit Character unlock mask, and Dash Growth level; missing or invalid data safely resets and writes BASIC-only unlocked, Coin 0, Growth 0. The Meta file is never deleted by New Game, Restart, GAME OVER, RUN CLEAR, or Run-save deletion, while `DeadSignal.sav` remains only the current Run checkpoint. GAME OVER grants exactly the current normal-Enemy `runKillCount`, RUN CLEAR grants `runKillCount + 3` prototype Coin, PRESSURE remains excluded, and a per-Run guard plus immediate Meta write prevents duplicate reward; Save & Title grants nothing. Character Select now displays Coin and contains five Characters, Dash Growth, and Back. BASIC is always unlocked; prototype one-time unlock costs are MOBILITY 5, PIERCER 10, HEAVY 15, and RAPID 20 Coin. A locked Character purchase deducts exact Coin and immediately persists but stays on Character Select, while a subsequent Z starts the Run; insufficient Coin produces compact feedback. Permanent Dash Growth uses prototype costs `5` for level `0 -> 1` and `10` for `1 -> 2`, stops at level 2/MAX, and persists immediately. All Characters have base Dash Capacity 1. Actual Capacity is `min(1 + Growth, Character cap)`, with prototype caps BASIC 3, MOBILITY 3, PIERCER 2, HEAVY 2, and RAPID 3. Dash now consumes one charge, blocks at zero, and sequentially restores one charge per existing recharge interval until full; DASH+ changes only that recharge interval to the existing `1.0/0.8/0.6-second` values and never increases Capacity. Character Dash distance and SILENT DASH remain unchanged. A fixed screen-space three-dot indicator displays current/max charge. New Game and Restart fully charge the selected Character's current Meta-derived Capacity; Room transitions preserve charges and recharge progress, and modal pauses stop recharge without delta-time jumps. Run Save version is now 8 and remains exactly `32 bytes` by using the former padding byte for Room-checkpoint Dash charges. Continue loads Character and charge from the Run Save, obtains unlock/Growth from Meta, recalculates the current cap, safely clamps stale charges, and starts a fresh recharge interval when below max rather than storing a mid-room fractional timer. Meta/default/invalid-file, reward-once, exact cost, cap, charge-consumption, zero-charge, sequential recharge, DASH+, Save round-trip, incompatible-version, and invalid-charge formula checks passed with zero failures. Isolated x64 UI/runtime validation passed for default Meta creation, invalid Meta recovery, insufficient-Coin rejection, MOBILITY purchase/reselection, both Dash Growth purchases/MAX, persistent Meta state, and v8 Character/charge checkpoint generation. H00 Room RNG inputs and generation remain untouched. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. Actual executable sizes are `54,272 bytes` for x86 (`+3,584 bytes` from H02) and `60,416 bytes` for x64 (`+1,536 bytes` from H02). Exact Coin rewards, prices, Character caps, and Dash balance remain prototype values for later review. `DeadSignal.sav` and `DeadSignal.meta` are runtime-generated user data and are not initial package contents. The final `1,474,560-byte` limit applies to the complete submission package including required runtime DLLs; final `/MD` package versus `/MT` standalone comparison remains required, and no packaging or size optimization was performed in H03. Do not implement the next stage until explicitly requested by the user.

`H04 Settings / Title / Save UX Completion` is complete. The functional Title flow is now Main `Game Start / Continue / Settings / Exit`; Game Start `New Game / Load / Back`; New Game opens Character Select; Continue and Load both use the same latest valid single `DeadSignal.sav` checkpoint without Character Select; missing or invalid Save stays in its current menu and shows the existing compact `No Save` feedback. Character Select Back and ESC now return to the Game Start submenu rather than Main Title. One shared Settings screen is reused from Title and Gameplay Menu with `Audio: ON/OFF` and Back. Up/Down selects, Left/Right or Z toggles Audio, and ESC/Back returns to Main Title or the still-open Gameplay Menu according to origin. Gameplay Settings is fully modal: simulation, Enemy AI, Trap, Spinner, Pressure, Detection, and Dash recharge remain paused, QPC is refreshed, and leaving Settings returns to Gameplay Menu rather than auto-resuming. Audio OFF rejects new generated-tone playback requests; Audio ON reuses the unchanged waveform/frequency/playback path. Audio persists in the existing version-1 `DeadSignal.meta` by using one former reserved byte while Meta remains exactly `16 bytes`: legacy H03 value 0 maps to default ON, new writes encode ON/OFF explicitly, and Coin/Unlock/Dash Growth remain unchanged. Missing or invalid Meta still creates the default profile with Audio ON. Gameplay Menu `Save & Title` writes only the existing persistent Room-checkpoint state and returns to Main Title; Enemy HP/position, Detection, A*, and Trap phase remain unsaved. Exit performs the normal Window close with no new auto-save. GAME OVER/RUN CLEAR reward-once, Run Save deletion, Meta/settings preservation, and same-Character Restart paths are unchanged. Formula/direct validation passed legacy Meta migration, ON/OFF persistence, invalid-Meta ON fallback, Coin/Unlock/Growth preservation, shared Settings entry/return, Audio-OFF request blocking, Character Select back routing, and v8 checkpoint integrity with zero failures. Isolated x64 runtime validation passed missing/invalid Continue rejection, Title Audio OFF/ON persistence, a 1.5-second Gameplay Settings modal pause with Dash charge unchanged, Settings-to-Gameplay-Menu return, Save & Title, Continue, Game Start Load, selected Character/charge restoration, and Title Exit. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. Run Save remains version 8 and `32 bytes`; Meta remains version 1 and `16 bytes`. Actual executable sizes are `54,784 bytes` for x86 (`+512 bytes` from H03) and `61,440 bytes` for x64 (`+1,024 bytes` from H03). `DeadSignal.sav` and `DeadSignal.meta` remain runtime-generated user data excluded from the initial package. The final `1,474,560-byte` limit applies to the complete package including required runtime DLLs; the `/MD` runtime package versus `/MT` standalone comparison remains deferred until the completed game, and no packaging or size optimization was performed in H04. Do not implement the next stage until explicitly requested by the user.

`H05 Content Completion Audit` is complete with no Gameplay, balance, presentation, project-setting, or size-optimization change. The implemented Core/Run/Room/Enemy/Combat/Upgrade/Character/Meta/UX loop is functionally closed: the six-Room seeded Run, count-derived Size, five Room Types, all five seeded normal Roles plus OPEN-only PRESSURE, fixed four-normal-plus-separate-Pressure storage, Upgrade gating, Character unlock, Dash Capacity, checkpoint Continue/Load, Audio persistence, reward-once Meta grants, GAME OVER/RUN CLEAR, Restart, Save & Title, and no-auto-save Exit paths are present. Size 1 and Size 5 are intentionally supported but unused by the current `2/2/3/3/4/4` progression and are not a content defect; internal seed generation and deterministic checkpoint reproduction are sufficient, so a visible seed/replay entry is not required for Content Complete. `MUST=1`: add one minimal discoverable controls/objective reference before Content Complete, covering Arrow movement, Z Slash/Confirm, X Dash, C Execute, ESC Menu/Back, and the counted-Enemy-to-Exit Room objective; no tutorial framework is required. `SHOULD`: explain Character sidegrade profiles at selection, explain Upgrade effects rather than only names/stacks, expose Run-end Coin gain/total more clearly, and clarify the Room-checkpoint nature of Save & Title; these do not block the playable loop. `LATER`: visible seed sharing/replay, richer contextual tutorial, Room-transition presentation, all deferred Room/Maze/density/Enemy-count/Role-weight/Combat/Character/Upgrade/Coin/Dash balance, final art/audio/effects/UI, frame pacing, and size optimization. Content Complete cannot be declared until the single MUST is implemented; proposed order is `H06 Minimal Controls & Objective Guide`, then Final Functional QA, then user-led Balance/Polish, and only afterward final packaging/size work. Existing H04 artifacts were byte-checked unchanged at x86 `54,784 bytes` and x64 `61,440 bytes`; Run Save is version 8/`32 bytes` and Meta is version 1/`16 bytes`. Both Release executables dynamically import `KERNEL32.dll`, `USER32.dll`, `GDI32.dll`, `WINMM.dll`, `VCRUNTIME140.dll`, and UCRT API-set runtime/math/stdio/locale/heap DLL names, confirming the current dynamic CRT dependency; Windows system DLLs are not candidate bundled files, while the exact app-local/runtime redistributable set remains to be established for the final supported target. The exact submission limit remains `1,474,560 bytes` for the whole package, not the EXE alone; compare `/MD` plus every required runtime/deployment file against `/MT` standalone only after the game is complete. Do not implement H06 or any other next stage until explicitly requested by the user.

`H06 Minimal Controls & Objective Guide` is complete. Title Main is now `Game Start / Continue / Settings / Help / Exit`, and Gameplay Menu is now `Resume / Settings / Help / Save & Title`; both reuse the existing keyboard/menu/modal rendering paths and open the same single screen-space Help view. Help states Arrow=`Move`, Z=`Slash / Confirm`, X=`Dash`, C=`Execute`, and ESC=`Menu / Back`, then explains the minimum objective `defeat every counted normal Enemy -> Exit unlock -> move through the open Exit`, including that the invulnerable OPEN PRESSURE Enemy is not a kill target. Z on the displayed Back item or ESC returns Title Help to Title Main and Gameplay Help to the still-open Gameplay Menu; Gameplay Help never resumes directly. Gameplay Help retains full modal pause for Player, AI, Detection, Trap, Spinner, PRESSURE, Dash recharge, Camera gameplay update, and the existing QPC refresh prevents accumulated resume delta. Help consumes Arrow navigation and routes Z only to Back; X/C cannot request Gameplay actions while the Gameplay Menu remains active, the hidden Gameplay Menu is not mouse-clickable through Help, and the existing release block remains intact on final Resume. The longest Help line measured `239 pixels` with the current default GDI font, fitting the `320x180` width, and all lines plus Back fit in the 180-pixel vertical layout. An isolated x64 input smoke traversed Title Help and Gameplay Help, injected Arrow/X/C while Help was active, held the Gameplay Help modal for `1.6 seconds`, returned through Gameplay Menu, resumed, and remained responsive; Meta stayed `16 bytes`. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. Actual executable sizes are x86 `55,808 bytes` (`+1,024 bytes` from H05) and x64 `62,464 bytes` (`+1,024 bytes` from H05). Run Save remains version 8/`32 bytes`, Meta remains version 1/`16 bytes`, and the dynamic CRT/link policy is unchanged. H05's only MUST is complete, so the game may enter Content Complete on a functional-scope basis; Final Functional QA remains the next proposed stage, and the H05 SHOULD/Polish backlog remains intentionally untouched. The final submission limit remains `1,474,560 bytes` for the complete runtime-inclusive package, with `/MD` package versus `/MT` standalone comparison deferred until the completed game. Do not implement the next stage until explicitly requested by the user.

`H07 Content Complete Final Functional QA` is complete with no Gameplay, balance, presentation, project-setting, or optimization change and no code fix required. Functional issue counts are `BLOCKER=0`, `MAJOR=0`, and `MINOR=0`; the game is now locked as Content Complete on the approved functional scope. The existing Balance/Polish backlog remains separate: Character/profile and Upgrade-effect explanation, clearer Run-end Coin and checkpoint feedback, visible seed/replay, contextual tutorial, Room/Maze/density/Enemy-count/Role-weight/Combat/Character/Upgrade/Coin/Dash tuning, final art/audio/effects/UI, and final frame-pacing judgment were not changed or reclassified as bugs. A Release validation executable directly reused the actual current `SetupCurrentRoom`, collision/navigation helpers, and `GenerateUpgradeOffer` without copying generator logic. Seeds `1..100,000` across all six Rooms covered `600,000` Rooms and produced `wrong Enemy count=0`, `wrong Room Size=0`, `duplicate Role=0`, `invalid spawn=0`, `Entity overlap=0`, `Wall/Trap overlap=0`, `Exit blocked=0`, `unreachable=0`, `invalid Facing/Home/Patrol=0`, `determinism mismatch=0`, and `OPEN PRESSURE placement failure=0`. Room Type counts were OPEN `119,954`, PILLAR `119,288`, MAZE `120,384`, TRAP `119,636`, and MIXED `120,738`; Role counts were PATROLLER `360,268`, WATCHER `359,619`, HUNTER `360,324`, LISTENER `360,160`, and SPINNER `359,629`. Upgrade stress across the same seed range, all three Upgrade Rooms, initial/Reroll, and eight availability states produced `duplicate option=0`, `maxed option=0`, `invalid ID=0`, `offer mismatch=0`, and `unchanged eligible Reroll=0`; all six Upgrade IDs appeared. Direct current-function validation produced `functional_failures=0` across twenty Dash-capacity/recharge checks, fifteen Character-profile/Upgrade composition checks, all three Functional Upgrade branches, v8 Save round-trip and incompatible-version rejection, Meta missing/invalid/default and persistent round-trip, and reward-once guards: GAME OVER with seven kills remained seven Coin after a duplicate call, and RUN CLEAR then produced only the expected additional ten. Actual x64 input soak ran `126.1 seconds`: Room 5 OPEN with SPINNER/LISTENER/WATCHER/PATROLLER plus PRESSURE `42.0 seconds`, Room 5 TRAP with WATCHER/SPINNER/LISTENER/HUNTER `42.0 seconds`, and Room 5 MIXED with SPINNER/LISTENER/PATROLLER/WATCHER `42.1 seconds`. Each case executed `78` actual WM_KEY movement/Dash/Slash/Execute and pause/resume cycles, remained responsive, retained a `32-byte` Save and `16-byte` Meta, and showed no crash, hang, observed path/state corruption, or obvious hitch. A separate actual UI route passed Title Settings/Help/Game Start/Back, Character Select/New Game, Gameplay Settings/Help/Save & Title, and Continue; Audio OFF persisted immediately, New Game and Save & Title produced valid v8/32-byte checkpoints, and Continue remained responsive. A runtime invalid-file test safely rebuilt invalid Meta as version 1 BASIC-only/Coin 0/Growth 0/Audio ON and rejected an unchanged invalid Save without leaving Title or crashing. x86 launch/window smoke passed resize to `800x500`, maximize, minimize, restore, responsiveness, and close. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. Executable sizes remain x86 `55,808 bytes` and x64 `62,464 bytes`; Run Save remains version 8/`32 bytes`, Meta remains version 1/`16 bytes`. Both PE files import `KERNEL32.dll`, `USER32.dll`, `GDI32.dll`, `WINMM.dll`, `VCRUNTIME140.dll`, and UCRT API-set runtime/math/stdio/locale/heap DLL names under the unchanged `/MD` policy. EXE-only headroom is `1,418,752 bytes` for x86 and `1,412,096 bytes` for x64, but these are not package budgets: the final submission limit is `1,474,560 bytes` for the complete runtime-inclusive package. Size optimization has not started; `/MD` plus required runtime files versus `/MT` standalone must be measured only after the user's manual Balance/Polish stage. The next stage is user-directed manual Balance/Polish, and no further implementation may begin until explicitly requested.

`B01 Run Scale & Room Generation Pass` is complete. The Run is now 12 Rooms with zero-based `roomIndex=0..11`, normal Enemy Count `2 + roomIndex * 2` (`2..24`), and fixed storage for 24 normal `EnemyRuntime` entries plus the unchanged separate OPEN-only PRESSURE runtime. PRESSURE remains invulnerable and excluded from normal Enemy Count, HUD, Exit unlock, and Run Kill Count. Room Size is derived from normal Enemy Count: `2..4 -> Size 2`, `6..10 -> Size 3`, `12..18 -> Size 4`, and `20..24 -> Size 5`; Size 1 remains unused in the normal Run. Individual Wall, Pillar, and Trap dimensions remain unscaled while the existing Size-based density counts extend naturally through Size 5. Normal Roles are independently selected by the existing deterministic Room RNG, so duplicate PATROLLER/WATCHER/HUNTER/LISTENER/SPINNER Roles are allowed; Role weights remain unimplemented and Role behavior is unchanged.

PILLAR now uses deterministic random valid coordinates while retaining the existing per-Size counts and fixed `16x24` Pillar size. MAZE divides placement around the central cross reference into four Quadrants, places one or two deterministic long Horizontal/Vertical segments per Quadrant, keeps fixed thickness, and guarantees both orientations in each generated MAZE without implementing a general maze algorithm. TRAP retains the existing Size-based counts and fixed `24x14` Trap size but distributes positions across the full Room. MIXED retains its existing per-Size Wall/Trap counts and randomly intermixes both across the full Room while preventing direct overlap. Exit selection is deterministic across LEFT/RIGHT/TOP/BOTTOM; left/right retain vertical Exit geometry and top/bottom use horizontal geometry at the world edge. Spawn and Exit corridors remain clear and Enemy navigation remains connected.

Upgrade remains after Rooms 2, 4, 6, 8, 10, and 12, with RUN CLEAR only after the Room-12 Upgrade. Exhaustive legal pre-sixth-choice stack/functional ownership states produced zero cases with fewer than two valid Upgrade options, so the existing pool, max-stack, offer, functional, and one-Reroll rules remain unchanged. Run Save remains version 8 and exactly `32 bytes`; Meta remains version 1 and exactly `16 bytes`. Continue regenerates all Room-derived state from the unchanged seed-plus-Room-index structure, including Count, Size, Type, Roles, layouts, Exit side, spawns, and Pressure presence.

Release validation directly reused the actual generation and navigation helpers. Seeds `1..100,000` across all 12 Rooms covered `1,200,000` generated Rooms with `wrong Enemy count=0`, `wrong Room Size=0`, `Enemy capacity overflow=0`, `invalid Role=0`, `determinism mismatch=0`, all Player/Enemy/Wall/Trap/Exit/Pressure spawn or overlap failures `=0`, `Wall/Trap overlap=0`, `Exit blocked=0`, `unreachable Player->Exit=0`, `unreachable Enemy=0`, Size-density count failure `=0`, MAZE Quadrant-count failure `=0`, and MAZE orientation failure `=0`. All four Exit sides and all five normal Roles appeared, and duplicate Roles appeared. Windows Application Control blocked direct execution of newly rebuilt x86 and x64 game EXEs in the current environment, so no new B01 interactive/soak claim is made; the Release validation DLL was executed through the system loader and completed. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. Actual executable sizes are x86 `55,296 bytes` (`-512 bytes` from H07) and x64 `61,440 bytes` (`-1,024 bytes` from H07); no size optimization or `/MD` versus `/MT` comparison was performed. Exact Room/Enemy balance remains subject to later reevaluation. The final package limit remains exactly `1,474,560 bytes`, including every required runtime/deployment file, not the EXE alone. B02 Enemy AI/FOV work and B03 Combat work must wait for explicit user instruction.

`B01.1 Room Generation Refinement` is complete. Larger Rooms retain density by increasing structure counts rather than scaling the fixed individual Pillar/Trap/MIXED obstacle sizes. Pure OPEN Rooms now generate zero obstacle Walls and contain only normal Enemies, optional PRESSURE, the Exit, and the Player; MIXED remains a separate obstacle-bearing type. PILLAR keeps deterministic seed-random placement and Size-stage counts `1/4/6/12/20`; TRAP keeps fixed `24x14` hazards with counts `1/3/7/12/19`; MIXED keeps fixed `16x24` obstacles plus `24x14` hazards with counts `1+1/2+2/4+4/7+7/12+11`, all distributed across the available Room area under the existing overlap rules.

MAZE still uses the central cross only as a four-Quadrant placement division. Each Quadrant selects `0..3` continuous seed-random Horizontal/Vertical long rectangular segments. For zero-based Room index `r=0..11`, prototype weights out of 128 are Wall counts `0: 12-r`, `1: 48-2r`, `2: 48+r`, and `3: 20+2r`, so the zero-Wall probability falls from `12/128` in Room 1 to `1/128` in Room 12 while later Rooms favor two or three Walls. Long-wall thickness is now fixed at `8 logical pixels` for both orientations, half the previous `16`; it does not scale with Room Size. Long-wall length is `50..70%` of its assigned Quadrant's corresponding axis span, so average Horizontal and Vertical length increases through Size 2/3/4/5 while every segment remains unbroken and inside its Quadrant. Each candidate is bounded by the existing outer/end margins, rejects Wall overlap and Player/Exit overlap, and uses at most 128 placement candidates. A complete candidate layout is accepted only when the Player navigation component contains every valid A* cell; otherwise generation deterministically retries at most 32 layouts, preserving Player-to-Exit and all subsequently selected Enemy reachability without a generic maze framework. Trap and MIXED random distribution and Wall/Trap separation remain unchanged.

Upgrade now appears only after Rooms `2/4/6/8/10`. Clearing Room 12, unlocking and entering its Exit sets RUN CLEAR immediately without opening Upgrade; existing Upgrade selection, one-Reroll, persistence, and Save rules remain unchanged. Save remains version 8 and exactly `32 bytes`, and identical `runSeed + roomIndex` still regenerates Type, Count, Size, Roles, all placements, MAZE count/orientation/length, and Exit side. Release validation covered seeds `1..100,000` across all 12 Rooms (`1,200,000` Rooms) with zero OPEN-Wall, requested-density, assigned-Quadrant, thickness, Wall/Wall, Wall/Trap, spawn/Exit/Pressure overlap, unreachable Player/Exit/Enemy, MAZE zero-weight trend, MAZE density growth, MAZE length growth, Run-flow, or determinism failures; both orientations, zero-Wall Quadrants, and all four Exit sides appeared. Release Rebuilds succeeded with zero warnings and zero errors. Actual B01.1 executable sizes are x86 `55,808 bytes` (`+512 bytes` from B01) and x64 `62,464 bytes` (`+1,024 bytes` from B01). No size optimization was performed. The final runtime-inclusive package limit remains exactly `1,474,560 bytes`. B02 and later work remains blocked until explicit user instruction.

`B02 Enemy AI Behavior Pass` is complete. A normal moving Enemy whose horizontal Patrol target is blocked by a Wall now checks the two directions at exactly 90 degrees from its current Facing, selects an available side deterministically when both are open, consumes the remaining Patrol-leg distance in that direction without teleporting, then walks back to the Patrol segment through the existing retained A* path and resumes Patrol. PATROLLER and HUNTER visual SUSPICION now retain the existing `0.5-second` lost-sight hold before walking toward the saved last-seen Player position with FOV/LOS still active; Detection decay begins only after the investigation point is reached, reacquisition resumes the existing Detection/ALERT flow, and full decay returns them to their Patrol area. HUNTER ALERT direct chase and retained A* Search movement now use `62 logical pixels per second` (`prototype`), while its `70` reacquire range and approximately `15-second` failed-search timeout remain unchanged.

All ALERT entry paths now reuse one minimal runtime reset helper. A LISTENER entering actual ALERT propagates once by a bounded `currentEnemyCount` array pass to living PATROLLER/HUNTER/LISTENER Enemies only; hearing-only SUSPICION does not propagate, WATCHER/SPINNER/PRESSURE are excluded, already-ALERT Enemies are unchanged, and duplicate LISTENER Roles cannot recursively broadcast. A successful Slash damage event immediately Alerts the surviving hit Enemy and Alerts every living normal Enemy within `80 logical pixels` (`prototype`) of the hit position, including WATCHER/SPINNER but excluding PRESSURE and dead Enemies. If that local transition includes a LISTENER, the separate LISTENER ALERT propagation rule is then applied without recursion. PRESSURE pursuit speed is now `32 logical pixels per second` (`prototype`) with its invulnerability, no-FOV, HUD/Exit exclusion, and combat exclusion unchanged.

The SUSPICION-to-CALM overlap stop was caused by `MoveEnemyToward` rejecting even separating movement while an Enemy was already overlapping the Player. It now continues to reject entry into Player overlap but permits movement that increases separation, so moving Roles can leave the overlap and follow their retained Home/Patrol return path; WATCHER/SPINNER stationary CALM behavior is unchanged. Fixed storage remains 24 normal Enemies plus the separate PRESSURE runtime, with no dynamic allocation or persistence field. Run Save remains version 8/`32 bytes`; Meta remains version 1/`16 bytes`.

Direct helper stress covered 24 Enemies, duplicate PATROLLER/HUNTER/LISTENER Roles, repeated Listener propagation, local Slash range boundaries, Watcher/Spinner inclusion, dead/Pressure exclusion, overlap escape, and Patrol 90-degree continuation with `failures=0`, no recursive Alert loop, and no array/path corruption. The unchanged B01.1 generator was revalidated over seeds `1..100,000` and all 12 Rooms (`1,200,000` Rooms), retaining zero generation, overlap, reachability, density, flow, or determinism failures. An isolated x64 Gameplay soak ran `92.2 seconds` and `245` Arrow/Z/X/C input iterations with the Window responsive throughout and no crash or hang; it produced the unchanged `32-byte` Save and `16-byte` Meta. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. Actual B02 executable sizes are x86 `58,368 bytes` (`+2,560 bytes` from B01.1) and x64 `66,560 bytes` (`+4,096 bytes` from B01.1). Exact AI speeds/ranges remain prototype balance for later review. Room generation, MAZE, Trap, MIXED, Exit, FOV shape, Execute angle, Player i-frame, Enemy wind-up, Character/Upgrade/Meta balance, and Save format were not changed. No size optimization was performed; the final runtime-inclusive package limit remains exactly `1,474,560 bytes`. B03 remains blocked until explicit user instruction.

`B03 Vision & Combat Readability Pass` is complete. Slash local combat Alert now uses a separate `40 logical pixel` prototype radius while direct Slash damage still enters ALERT immediately; nearby living normal Roles, including WATCHER/SPINNER, are included, while dead Enemies and PRESSURE remain excluded. LISTENER movement Hearing remains `80 logical pixels` and Dash Hearing remains `120 logical pixels`. Detection and FOV rendering now share the same circular-sector geometry: radius `70`, total angle `55 degrees` (`+/-27.5 degrees`), radial arc boundary, and existing Wall/Pillar LOS clipping; Traps remain non-blocking, CALM/SUSPICION red-blue progress and decay remain, and ALERT still hides FOV. Execute keeps its current base/Upgrade reach and nearest-CALM-normal-target rule but now accepts approximately `170 degrees` around Player Facing (`+/-85 degrees`), with rear targets and PRESSURE excluded.

Player damage now resolves once per Gameplay Update using only the highest pending Damage, then starts a `0.5-second` prototype invulnerability timer; the timer advances only in active Gameplay, so Settings/Help/Gameplay Menu/Upgrade/GAME OVER/RUN CLEAR pauses do not consume it or cause a Resume jump. Normal ALERT Enemy and PRESSURE contact attacks share an approximately `0.3-second` prototype stationary wind-up. The runtime silhouette changes to an orange/yellow alternating pattern during wind-up; no Damage occurs before completion, range is checked again at the strike moment so departure causes a miss, death/disabled attack cancels the wind-up, and the existing attack cooldown and Damage values remain unchanged. Save remains version 8/`32 bytes` and Meta remains version 1/`16 bytes`; neither transient timer is persisted.

Direct B03 validation returned `failures=0` for Slash 40-pixel boundaries, 80/120 Hearing separation, circular-sector radius/angle/arc cases, Wall clipping, Execute inside/outside/rear cases, highest-only `1+2+1 -> 2` Damage, wind-up delay/hit/miss/death cancellation, and 24-Enemy capacity. A dedicated x64 90,000-millisecond stress completed `1,366,660,095` repeated 24-Enemy attack/Alert/FOV updates with `soak_failures=0`. The unchanged Room generator was also revalidated across seeds `1..100,000` and all 12 Rooms (`1,200,000` Rooms) with zero determinism, capacity, placement, overlap, reachability, density, MAZE, Exit, Upgrade-flow, or Run-flow failures. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. Actual B03 executable sizes are x86 `59,392 bytes` (`+1,024 bytes` from B02) and x64 `67,072 bytes` (`+512 bytes` from B02). No size optimization was performed; the final runtime-inclusive package limit remains exactly `1,474,560 bytes`. Exact vision/combat timing and ranges remain available for later balance review. B04 remains blocked until explicit user instruction.

`B04 Alert Network / Dash / Run Reward Pass` is complete. A successful Slash Damage event uses the actually hit Enemy position as the unchanged `40 logical pixel` combat-noise center and Alerts every living normal PATROLLER/HUNTER/LISTENER/WATCHER/SPINNER in range; the surviving directly hit Enemy enters ALERT immediately, while dead Enemies and PRESSURE remain excluded. A LISTENER newly entering ALERT through visual Detection, direct Slash Damage, Slash combat noise, or another LISTENER relay emits its separate pure-distance `30 logical pixel` Alert to living PATROLLER/HUNTER/LISTENER only. WATCHER/SPINNER/PRESSURE/dead Enemies are excluded. Relay is enabled through a bounded fixed queue of at most 24 Enemy indices: each newly Alerted LISTENER propagates once for that ALERT entry, already-ALERT LISTENERs do not rebroadcast, and A-B-C chains complete without recursion or per-frame broadcast.

Each Room now first assigns the exact prototype LISTENER target by Room Size `Size1/2/3/4/5 = 2/2/3/4/4`, clamped to current normal Enemy Count, then selects every remaining Role from PATROLLER/WATCHER/HUNTER/SPINNER with the existing deterministic Room RNG; LISTENER therefore never exceeds the target and remains capped at 4. Existing Enemy spawn validation is reused unchanged, so LISTENER starts cannot share a position or overlap another LISTENER, Wall/Pillar, Trap, Player, or Exit; no minimum 30-pixel separation rule was added. Run-scoped `alertEventCount` increments only when a living normal Enemy enters ALERT while no Alert Event is active. The Event remains active while any living normal Enemy is ALERT or SUSPICION and ends only when all living normal Enemies are fully CALM; PRESSURE and dead Enemies are excluded. GAME OVER and RUN CLEAR retain `runKillCount` and the existing Clear Bonus, then multiply the final earned reward by 2 only when `alertEventCount == 0`; the existing per-Run payout guard and Meta accumulation remain unchanged.

The existing Dash implementation was verified to use `min(1 + DashGrowthLevel, CharacterDashCap)`, producing Growth `0/1/2` capacities BASIC `1/2/3`, MOBILITY `1/2/3`, PIERCER `1/2/2`, HEAVY `1/2/2`, and RAPID `1/2/3`. Three charges consume successfully in sequence, zero blocks Dash, missing charges recharge one at a time, DASH+ changes only recharge duration, Room transition does not refill, New Game/Restart starts full, and modal pause stops recharge. The maximum one-to-three runtime-generated charge blocks now follow the Player immediately beside the existing world-space HP bar and directly reflect current/max charge. Run Save remains version 8 and exactly `32 bytes` by reusing its former reserved byte for checkpoint `alertEventCount`; Continue restores it while retaining Room-start rollback for transient Enemy state. Meta remains version 1 and exactly `16 bytes`, with no Run alert field.

Direct B04 validation passed on x86 and x64 with `failures=0` for Slash/Listener boundaries and Role filters, direct and indirect Listener entry, A-B-C relay, reciprocal no-loop behavior, Alert Event grouping/end/restart, zero-Alert GAME OVER/RUN CLEAR reward doubling, one-plus-Alert unchanged reward, payout-once, Save round-trip, every Character/Growth Dash capacity, three-charge consumption/zero blocking, and sequential recharge. Seeds `1..100,000` across all 12 Rooms (`1,200,000` Rooms) produced Listener target failure `0`, Listener overlap `0`, invalid Role `0`, determinism mismatch `0`, and zero existing generation/placement/reachability/density/MAZE/Exit/flow failures. A dedicated x64 90,000-millisecond fixed-storage stress completed `372,520,502` repeated 24-Enemy/4-LISTENER relay cycles with `soak_failures=0`, no crash, hang, infinite propagation, or array corruption. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. Actual B04 executable sizes are x86 `59,904 bytes` (`+512 bytes` from B03) and x64 `67,072 bytes` (`+0 bytes` from B03). No size optimization, new source/header, external Library, Manager/framework/event system, or dynamic allocation was added. The final runtime-inclusive package limit remains exactly `1,474,560 bytes`. Do not implement the next stage until explicitly requested by the user.

`B05 Alert Range Tuning & Integration QA` is complete. Slash combat noise now uses the actually hit Enemy position and a pure-distance `65 logical pixel` radius, Alerting every living normal PATROLLER/HUNTER/LISTENER/WATCHER/SPINNER in range while excluding dead Enemies and PRESSURE. A surviving directly hit Enemy still enters ALERT immediately. A LISTENER newly entering ALERT now relays once from its current position through a pure-distance `70 logical pixel` radius to living PATROLLER/HUNTER/LISTENER only; WATCHER/SPINNER/PRESSURE/dead Enemies remain excluded. The existing bounded fixed queue preserves A-B-C-D relay without recursion, per-frame rebroadcast, reciprocal loops, or direct whole-Room Alerting, and a LISTENER can relay again only after fully returning to CALM and newly entering ALERT. Room Size LISTENER targets remain `Size1/2/3/4/5 = 2/2/3/4/4`, capped at 4 with existing spawn-overlap rejection and no new minimum-distance rule. Alert Event grouping/count/end semantics, zero-Alert Coin doubling and payout-once, every Character/Growth Dash capacity and sequential recharge/HUD, FOV `70`/`55 degrees`, Execute `+/-85 degrees`, damage/wind-up, Enemy/Room/Maze/Exit/Upgrade, Save v8 `32 bytes`, and Meta v1 `16 bytes` remain unchanged.

Direct B05 validation passed on x86 and x64 with `failures=0`. Slash distance boundaries `64/65/66` produced ALERT/ALERT/no Slash ALERT; LISTENER distance boundaries `69/70/71` produced relay/relay/no relay. The `A-B=69`, `B-C=69`, `A-C>70` chain reached C, while WATCHER/SPINNER were excluded from LISTENER relay but included in Slash noise. Role/dead/PRESSURE filters, one-broadcast-per-entry behavior, reciprocal no-loop behavior, Alert Event grouping/end/restart, Coin payout-once, Dash capacity/recharge, Save, and Meta checks passed. Seeds `1..100,000` across all 12 Rooms (`1,200,000` Rooms) produced zero Enemy count/capacity, LISTENER count, spawn-overlap, determinism, path/Exit, placement/reachability, density, MAZE, Upgrade-flow, or Run-flow failures. A dedicated x64 `90,000 ms` 24-Enemy/4-LISTENER integration soak completed `367,099,202` Slash-65/LISTENER-70 relay/Event/Dash/FOV/attack-wind-up/PRESSURE-filter cycles with `soak_failures=0`, no crash, hang, infinite relay, detected array corruption, duplicate Alert Event count, reward double payout, or Dash corruption. Release x86/x64 Rebuilds succeeded. Actual B05 executable sizes are x86 `59,904 bytes` (`+0 bytes` from B04) and x64 `67,072 bytes` (`+0 bytes` from B04). No size optimization was performed; the final runtime-inclusive package limit remains exactly `1,474,560 bytes`. Do not implement the next Balance stage until explicitly requested by the user.

`B06 Character Balance Preparation & Alert Final Tuning` is complete. Slash combat noise now uses the actually hit Enemy position and a pure-distance `70 logical pixel` radius for every living normal PATROLLER/HUNTER/LISTENER/WATCHER/SPINNER, excluding PRESSURE and dead Enemies; a surviving directly hit Enemy still enters ALERT immediately. A LISTENER newly entering ALERT now relays once through a pure-distance `80 logical pixel` radius to living PATROLLER/HUNTER/LISTENER only. WATCHER/SPINNER/PRESSURE/dead Enemies remain excluded, and the existing bounded fixed queue preserves A-B-C-D relay without LOS/Wall blocking, recursive propagation, reciprocal loops, or per-frame rebroadcast. LISTENER Room Size targets remain `Size1/2/3/4/5 = 2/2/3/4/4`, capped at 4 with unchanged overlap rejection and no minimum-distance rule.

BASIC remains the Character balance baseline. The audited runtime profiles below match the existing H02/H03 AGENTS records; B06 changed no Character or Upgrade value. Move and cooldown are the zero-Upgrade actual results, Slash shape is forward reach x lateral width, and Dash Cap is the Character ceiling before applying `min(1 + Growth, cap)`.

| Character | Max HP | Move | Slash CD | Damage | Reach x Width | Target Cap | Dash Distance | Dash Cap |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| BASIC | 10 | 60 | 0.500 s | 1 | 8 x 8 | 3 | 32.0 | 3 |
| MOBILITY | 9 | 66 | 0.450 s | 1 | 7 x 8 | 2 | 35.2 | 3 |
| PIERCER | 8 | 63 | 0.550 s | 1 | 12 x 4 | 1 | 32.0 | 2 |
| HEAVY | 12 | 51 | 0.625 s | 2 | 7 x 8 | 4 | 28.8 | 2 |
| RAPID | 9 | 63 | 0.375 s | 1 | 7 x 8 | 2 | 32.0 | 3 |

Relative to BASIC, MOBILITY is Move `+10%`, HP `-1`, Slash cooldown `-10%`, reach `-1`, Target Cap `-1`, and Dash Distance `+10%`; PIERCER is Move `+5%`, HP `-2`, Slash cooldown `+10%`, reach `+50%`, width `-50%`, Target Cap `-2`, and Dash Cap `-1`; HEAVY is HP `+2`, Move `-15%`, Slash cooldown `+25%`, Damage `+100%`, Target Cap `+1`, reach `-1`, Dash Distance `-10%`, and Dash Cap `-1`; RAPID is Move `+5%`, HP `-1`, Slash cooldown `-25%`, reach `-1`, and Target Cap `-1`. Each non-BASIC profile therefore retains both advantages and disadvantages. Execute remains common, CALM-only, nearest-one, PRESSURE-excluded, `+/-85 degrees`, with reach `4` or `8` under EXECUTE REACH.

Direct B06 validation passed on x86 and x64 with `failures=0`. Slash boundaries `69/70/71` produced ALERT/ALERT/no Slash ALERT; LISTENER boundaries `79/80/81` produced relay/relay/no relay; and `A-B=79`, `B-C=79`, `A-C>80` reached C once without loops. All five Characters passed Max HP, Move, Slash cooldown/damage/shape/cap, Dash distance, New Game/Restart-style base reconstruction, and every Character x Growth `0/1/2` Dash capacity check. A 24-normal-Enemy Slash selection test preserved the current normal-Enemy array-order selection, damaged no more than each profile's simultaneous Target Cap, excluded dead Enemy storage and the separate PRESSURE Entity, applied HEAVY Damage 2, and matched PIERCER's longer/narrower collision and procedural outline. MOBILITY+MOVE+, HEAVY+SLASH+, PIERCER+EXECUTE REACH, RAPID+SLASH+/DASH+/SILENT DASH, HEAVY+FIELD MEDIC, and all-Character v8 Continue round trips retained the active Character profile without BASIC overwrite. The Dash HUD continues to use the same runtime `currentDashMaxCharges` obtained from `CurrentDashCapacity`. Alert Event grouping/end/restart, zero-Alert Coin doubling, payout-once, FOV `70`/`55 degrees`, Execute, i-frame, highest-only simultaneous Player damage, Enemy wind-up/speeds/investigation, Room/Upgrade flow, Save v8 `32 bytes`, and Meta v1 `16 bytes` passed unchanged.

Seeds `1..100,000` across all 12 Rooms (`1,200,000` Rooms) produced zero Enemy count/capacity, LISTENER count, spawn-overlap, determinism, path/Exit, placement/reachability, density, MAZE, Upgrade-flow, or Run-flow failures. A dedicated x64 `90,000 ms` 24-Enemy/4-LISTENER integration soak cycled all five Character profiles and Upgrade stacks while exercising Slash-70, LISTENER-80 relay, Alert Event, Dash charges, FOV, attack wind-up, and PRESSURE filtering for `370,210,370` iterations with `soak_failures=0`; there was no crash, hang, propagation loop, detected Character/Dash state corruption, Alert Count duplication, or array overflow. Release x86/x64 Rebuilds succeeded with zero warnings and zero errors. Actual B06 executable sizes are x86 `59,904 bytes` (`+0 bytes` from B05) and x64 `67,072 bytes` (`+0 bytes` from B05). No optimization, new Manager/framework/file/library, Character rebalance, Upgrade change, or Room/Maze/Enemy-AI structure change was made. The final runtime-inclusive package limit remains exactly `1,474,560 bytes`. Further Character balance waits for user play feedback.

`B07 Character Balance Pass 1` is complete without changing any Character, Upgrade, Enemy, Room, Maze, or Alert value. BASIC remains the `100%` balance baseline. The current source still produces the B06 zero-Upgrade profiles exactly: BASIC `HP 10 / Move 60 / Slash 0.500 s, Damage 1, 8x8, Cap 3 / Dash 32.0, Cap 3`; MOBILITY `9 / 66 / 0.450 s, 1, 7x8, 2 / 35.2, 3`; PIERCER `8 / 63 / 0.550 s, 1, 12x4, 1 / 32.0, 2`; HEAVY `12 / 51 / 0.625 s, 2, 7x8, 4 / 28.8, 2`; and RAPID `9 / 63 / 0.375 s, 1, 7x8, 2 / 32.0, 3`.

| Character | HP | Move | Slash frequency | Damage/hit | Single-target DPS | Nominal Slash area | Target Cap delta | Dash distance | Dash Cap delta |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| BASIC | 100% | 100% | 100% | 100% | 100% | 64 / 100% | 0 | 100% | 0 |
| MOBILITY | 90% | 110% | 111.1% | 100% | 111.1% | 56 / 87.5% | -1 | 110% | 0 |
| PIERCER | 80% | 105% | 90.9% | 100% | 90.9% | 48 / 75% | -2 | 100% | -1 |
| HEAVY | 120% | 85% | 80% | 200% | 160% | 56 / 87.5% | +1 | 90% | -1 |
| RAPID | 90% | 105% | 133.3% | 100% | 133.3% | 56 / 87.5% | -1 | 100% | 0 |

The current normal Enemy starts at exactly `3 HP`. Fresh-Enemy basic-Slash hit counts and minimum theoretical times from the first immediate hit are BASIC `3 / 1.000 s`, MOBILITY `3 / 0.900 s`, PIERCER `3 / 1.100 s`, HEAVY `2 / 0.625 s`, and RAPID `3 / 0.750 s`. HEAVY Damage 2 therefore crosses the exact `3 HP` breakpoint: despite its 80% attack frequency, it has 160% BASIC single-target theoretical DPS and the shortest fresh-Enemy kill time. This is recorded as a clear numeric breakpoint, not changed without user approval. With `1/2/3/4` valid targets inside one Slash, total per-Slash damage is BASIC `1/2/3/3`, MOBILITY `1/2/2/2`, PIERCER `1/1/1/1`, HEAVY `2/4/6/8`, and RAPID `1/2/2/2`.

MOVE+ remains a multiplicative `1.1x/1.2x` at stack `1/2`: BASIC `66/72`, MOBILITY `72.6/79.2`, PIERCER `69.3/75.6`, HEAVY `56.1/61.2`, and RAPID `69.3/75.6`. SLASH+ remains `0.8x/0.6x`: BASIC `0.400/0.300 s`, MOBILITY `0.360/0.270 s`, PIERCER `0.440/0.330 s`, HEAVY `0.500/0.375 s`, and RAPID `0.300/0.225 s`. DASH+ remains the common recharge duration `0.8/0.6 s`, changing neither Character Dash distance nor Capacity. No stack reaches zero or an invalid value, and uniform multiplicative scaling preserves the base Move/Slash ordering. SILENT DASH, EXECUTE REACH, and Character-Max-HP-aware FIELD MEDIC regression passed.

No Character implementation error or Target Cap error was found. The existing 24-normal-Enemy array-order Slash test limits hits to each profile's Cap, excludes dead storage and PRESSURE, applies HEAVY Damage 2, and confirms PIERCER's rotated `12x4` collision and procedural outline. The non-PIERCER `7x8` profiles use `7x8` for cardinal attacks, but the existing axis-aligned diagonal branch produces a matching `7x7` visual and collision rectangle because both active axes use Reach; there is no render/hit mismatch. Changing that established diagonal geometry requires a design decision, so B07 records it for directional play testing rather than changing it silently. Manual balance testing is also required for HEAVY's `3 HP` breakpoint versus its movement/Dash exposure, RAPID's 133.3% cadence versus HP/area/Cap losses, PIERCER's long-Reach safety value versus its 90.9% DPS/HP/width/Cap losses, and MOBILITY's combined movement/Dash advantage versus HP/area/Cap losses.

Direct current-function validation passed on x86 and x64 with `failures=0`, covering the five profiles, New Game/Restart-style reconstruction, Slash damage/shape/24-Enemy Cap, Dash Capacity and recharge, common Execute/Functional Upgrade behavior, representative Upgrade combinations, all-Character v8 Continue round trips, and Slash-70/LISTENER-80 Alert regression. Isolated actual Release UI/input smoke selected every Character on both architectures, confirmed each generated 32-byte checkpoint's Character ID and profile Max HP, sent movement/Slash/Dash/Execute input, remained responsive, and exited normally; user Save/Meta were not touched. Release x86/x64 Rebuilds succeeded. Actual executable sizes remain x86 `59,904 bytes` (`+0` from B06) and x64 `67,072 bytes` (`+0`); Run Save remains version 8/`32 bytes` and Meta remains version 1/`16 bytes`. No `DeadSignal.cpp` change, optimization, new system/file/library, or balance change was made. Further Character number changes require user approval.

`B08 Meta Progression & Preparation UI Foundation` is complete. The B07 Character base profiles remain unchanged. Alert-based Coin doubling was removed completely: `alertEventCount` has no reward effect and remains a version-8 checkpoint field and Run Result statistic. The reward order is Kill/Clear base reward, then Common COIN SENSE `+10%/+20%` with integer truncation, then one-time payout. Result COIN displays the exact credited amount, including saturation behavior.

The fixed Common progression is RUN REROLL Lv0/1=`1/2` uses, FIELD RECOVERY Lv0/1=`none/+1 HP after Upgrade Rooms 2/4/6/8/10 capped at Run max HP`, and COIN SENSE Lv0/1/2=`100/110/120%`. BASIC is the only fresh-Meta unlocked Character; MOBILITY/PIERCER/HEAVY/RAPID retain prototype unlock costs `5/10/15/20`. Prototype Common costs are RUN REROLL `10`, FIELD RECOVERY `10`, and COIN SENSE `5/10`. These and all Character progression costs are explicitly prototype values for later balance work.

Each Character owns an independent three-entry fixed progression. BASIC has DASH CAP `1/2/3`, VITALITY `base/+1/+2`, and BALANCED EDGE `base/+1 Reach/+1 Width at Lv2`; MOBILITY has DASH CAP `1/2/3/4`, LIGHT FRAME `1.00/1.05/1.10`, and LONG STEP `1.00/1.10/1.20`; PIERCER has DASH CAP `1/2`, LONG POINT `base/+2/+4 Reach only`, and SAFE DISTANCE `1.00/1.05/1.10`; HEAVY has fixed Dash 1, FORTIFY `base/+2/+4 HP`, WIDE SWING `base/+2/+4 Width only`, and STEADY BODY `1.00/1.05/1.10`; RAPID has DASH CAP `1/2`, TEMPO `1.00/0.90/0.80 cooldown`, and LIGHT BODY `1.00/1.05/1.10`. Character upgrade costs are prototype `5/10/15` for the next Lv1/Lv2/Lv3. Runtime order is Character Base, selected Character Global progression only, then Run Upgrade modifiers; Global and Run Upgrade data remain separate.

Title Game Start now opens Preparation with Coin, the Common list, Character, Run Start, and Back. Character uses the requested `25% slots / 25% preview / 50% Character Global Upgrade` layout, fixed focus state, Arrow/Z/ESC navigation, locked purchase, selected marker, current HP/Move/Dash preview, costs, LOW/MAX feedback, Character upgrade purchase with immediate Meta save, Run Start, and Back. Run Start uses only an unlocked selected Character. Continue still loads the checkpoint Character directly without Preparation. GAME OVER and RUN CLEAR reuse one Result layout showing Character, Room/12, Kill, Alert event count, earned Coin, total Coin, Restart, and Title.

Meta is now version 2 and exactly `32 bytes`, storing Coin, unlock bits, Audio, three Common levels, and fifteen independent Character levels. A valid version-1 `16-byte` Meta preserves Coin/unlocks/Audio, migrates its old Dash Growth only to BASIC DASH CAP, initializes all other new progression to Lv0, and is rewritten immediately as v2. Invalid/corrupt data still returns to BASIC-only, zero-Coin, Audio-ON safe defaults. Run Save remains version 8 and `32 bytes`.

Direct B08 validation passed on x86 and x64 with `b08_failures=0`, covering fresh/v2/v1/corrupt Meta, persistence, independent progression, all caps and derived stats, Character Base-to-Global-to-Run order, purchases, LOW/MAX, Preparation/Character/Run Start state transitions, Alert event lifecycle, Alert-independent rewards, COIN SENSE, payout once, Result values, 24-target Cap totals, and all-Character Save v8 round trips. B03 combat/FOV/Alert regression passed on x86 and x64 with zero failures, and the x64 B01 run generated `1,200,000` Rooms with zero count/capacity/Role/spawn/overlap/path/Exit/density/Maze/Upgrade/Run-flow failures. The x64 fixed-storage stress ran `90,000 ms`, completed `11,065,275,290` iterations with `soak_failures=0`, and found no invalid stat/cooldown/cap value. Slash noise `70`, LISTENER alert/relay `80`, FOV, Enemy, Room, Maze, Character base values, Damage, Target Cap, and Run Upgrade values remain unchanged. Release Rebuilds succeeded with zero warnings/errors. Actual B08 sizes are x86 `65,536 bytes` (`+5,632`) and x64 `73,216 bytes` (`+6,144`) from B07; final runtime executable remains far below the exact `1,474,560-byte` package limit. Do not rebalance Character base stats or prototype progression costs before user direction.

`B09 Character Identity & Global Progression Integration` is complete. BASIC remains the unchanged baseline. The approved base profiles are BASIC `HP10 / Move60 / Slash 0.500 s / Damage1 / Reach x Width 8x8 / Target3 / Dash32.0`; MOBILITY `8 / 72 / 0.500 s / 1 / 7x7 / 2 / 35.2`; PIERCER `7 / 57 / 0.600 s / 1 / 16x3 / 1 / 32.0`; HEAVY `14 / 45 / 0.750 s / 2 / 6x12 / 4 / 28.8`; and RAPID `8 / 66 / 0.250 s / 1 / 6x6 / 2 / 32.0`. Damage remains exactly `1/1/1/2/1`. The normal Enemy remains HP3, so hit-count/minimum-time-from-an-immediate-first-hit is BASIC `3/1.000 s`, MOBILITY `3/1.000 s`, PIERCER `3/1.200 s`, HEAVY `2/0.750 s`, and RAPID `3/0.500 s`; the approved HEAVY Damage-2 breakpoint is unchanged.

Character Global progression remains independent in Meta v2 and still applies as `Base Character Profile -> selected Character Global Upgrade -> per-Run Upgrade`. Maximum Global-derived profiles are BASIC `HP12 / Move60 / CD0.500 / 9x9 / Dash32.0 / Cap3`; MOBILITY `HP8 / Move79.2 / CD0.500 / 7x7 / Dash42.24 / Cap4`; PIERCER `HP7 / Move62.7 / CD0.600 / 20x3 / Dash32.0 / Cap2`; HEAVY `HP18 / Move49.5 / CD0.750 / 6x16 / Dash28.8 / Cap1`; and RAPID `HP8 / Move72.6 / CD0.200 / 6x6 / Dash32.0 / Cap2`. Final Dash capacities are exactly BASIC `3`, MOBILITY `4`, PIERCER `2`, HEAVY fixed `1`, and RAPID `2`. BASIC progression does not affect MOBILITY, MOBILITY progression does not affect RAPID, and HEAVY has no Dash progression.

Relative to BASIC at base Lv0, HP is `100/80/70/140/80%`, Move is `100/120/95/75/110%`, Slash frequency is `100/100/83.3/66.7/200%`, single-target theoretical DPS is `100/100/83.3/133.3/200%`, nominal Slash area is `100/76.6/75/112.5/56.25%`, Target Cap is `100/66.7/33.3/133.3/66.7%`, Dash distance is `100/110/100/90/100%`, and final Dash Cap is `100/133.3/66.7/33.3/66.7%`, in BASIC/MOBILITY/PIERCER/HEAVY/RAPID order.

Preparation Preview reads Global-derived HP/Move/current Dash Cap directly and excludes Run Upgrades. New Run and Restart start at the selected Character's Global-derived full HP; Continue retains the checkpoint Character and validates against its current Global-derived Max HP; Result uses the same Character name. The existing HUD supports one through four Character-specific Dash slots and all filled/empty states without overlap failure. Cardinal Slash geometry and collision/render bounds are `8x8`, `7x7`, `16x3`, `6x12`, and `6x6`; PIERCER uses a shared half-open lateral boundary so odd Width 3 renders and hits as exactly three pixels. Global geometry extends only the specified axis. MOBILITY+LIGHT FRAME+MOVE+, PIERCER+LONG POINT+EXECUTE REACH, HEAVY+WIDE SWING+SLASH+, RAPID+TEMPO+SLASH+, and BASIC+BALANCED EDGE passed; RAPID's maximum TEMPO plus maximum SLASH+ remains valid at `0.120 s`.

Meta remains version 2/`32 bytes`; Run Save remains version 8/`32 bytes`; v1 migration, fresh/corrupt defaults, purchase/relaunch persistence, and all independent Character levels remain compatible. Prototype unlock/Common/Character Upgrade costs are unchanged, and UI visual polish is deferred. Alert has no Coin effect; Kill/Clear then COIN SENSE remains the only reward formula. Enemy, Room, Maze, Alert, FOV, Execute, i-frame, wind-up, Hunter, Pressure, and Room/Upgrade flow values are unchanged. Direct B09 validation passed on x86/x64 with `b09_failures=0`; B03 combat/FOV/Alert regression passed on both architectures; x64 B01 regenerated `1,200,000` Rooms with zero count/capacity/Role/spawn/overlap/path/Exit/density/Maze/Upgrade/Run-flow failures; and the x64 `90,000 ms` fixed-storage stress completed `10,825,411,368` iterations with `soak_failures=0`. Release x86/x64 Rebuilds completed with zero warnings/errors, and both executables passed startup/responsiveness/close smoke. Actual B09 sizes are x86 `65,536 bytes` (`+0`) and x64 `73,216 bytes` (`+0`) from B08. Do not adjust Character numbers, prototype costs, or UI presentation again before user direction.

The current minimal foundation is functional Title/Preparation/Character Select/Run Start/Continue/Settings/Help/Exit UX + Common and five independent Character persistent Global progressions + persistent Audio ON/OFF + shared Run Result + Window + Real-time Loop + Software Framebuffer + Flicker-Reduced Win32 Repaint + Keyboard and Minimal Mouse Input + Runtime Generated Audio + Five-Stage Room Scale + Camera + Gameplay HUD/Menu with modal Settings/Help and Room-checkpoint Save & Title + Character-Max-HP-aware Player World-Space Health Bar and Dash-charge dots + Player Display/Movement/Eight-Direction Facing + Character-profile Slash/Dash and common Execute + Screen Boundary + Density-Adjusted Room Wall Collision + Seeded Periodic Floor Traps + Seeded Mixed Obstacle/Trap Rooms + Seeded two-to-twenty-four counted Runtime Enemies with duplicate Roles allowed from PATROLLER/WATCHER/HUNTER/LISTENER/SPINNER + One uncounted invulnerable PRESSURE Entity in OPEN Rooms + Independent CALM Patrol/Fixed or Rotating Stationary Surveillance/Route Return + Independent Visual/Hearing SUSPICION Investigation + Role-Adjusted Independent ALERT Direct Chase/A* Search/Scan/Contact Attack + Continuous PRESSURE Direct/A* Player pursuit + Dynamic Continuous Enemy Vision with Room-Wall Occlusion + Minimal Detection/HP/Damage/Death + Minimal Room Complete + Minimal Twelve-Room Run + six-entry deterministic per-Run Upgrade selection with one or two Rerolls + Version-8 Run Checkpoint Save/Continue + Version-2 permanent Meta/settings profile with v1 migration.

The final Architecture, integer-scaling/pixel-perfect policy, Window size policy, final Player pixel art, palette, Animation frame specification, actual Wall/Tile specification, need for a general Collision structure, Room structure, final Enemy visual/collision specification, Enemy-specific sprite specification, role-specific Enemy design, final Enemy AI/speed/Patrol rules, final Vision/FOV values and representation, final Player Detection geometry rule, Wall Occlusion/LOS design, final Execute reach conversion and presentation, relationship between gameplay width W and visual width, FPS/timing/pacing policy, Audio API, and actual sound specifications remain `미정`. Do not implement a later stage until explicitly requested.

## Development Principles

- Do not use Unity or another general-purpose game engine.
- Build an ultralight custom framework in Windows Native C++.
- Implement only functionality that `DEAD SIGNAL` actually needs.
- Do not add features or abstractions in anticipation of future reuse.
- Reuse the existing structure when it can satisfy the request; do not add a new system without demonstrated need.
- Prefer removing unused functionality over merely disabling it.
- Do not add an external runtime or library without comparing its measured final-build cost against its concrete benefit.
- Do not increase binary size solely for generality or architectural elegance.
- Do not apply a size optimization merely to save a few bytes if it would materially harm development stability or game completeness.
- Treat the C++ standard version, CRT linkage/use, character set, SDL checks, STL use, architecture, compiler options, and linker options as undecided final specifications. Current project settings are only the present baseline state. Adopt final settings only after measuring size and implementation benefit.
- MSVC is the initial compiler baseline. The final choice between Win32 (x86) and x64 is undecided.
- Do not add empty directories or hierarchy for anticipated expansion.

## Do Not Prebuild General Systems

Do not implement or prepare structures for the following until a concrete need is confirmed:

- General-purpose Scene System
- ECS
- Reflection
- General-purpose UI Framework
- General-purpose Animation Framework
- General-purpose Physics Engine
- Plugin System
- Runtime Scripting
- General-purpose Renderer abstraction
- General-purpose Input abstraction
- General-purpose Audio Engine
- General-purpose Asset Manager
- General-purpose Platform abstraction
- Unnecessary Utility or Math Library
- Separate Test Framework or Test Project
- Unnecessary multiplatform structure

## Build-Size Measurement Rules

Base technical decisions on actual final build results, not estimates. Whenever possible, compare each change as:

`current Build Size -> minimal implementation -> new Build Size -> byte increase/decrease`

- Record executable and submission sizes as exact byte counts.
- Define and report exactly which artifacts are included in the measured final submission.
- Record the compiler, target architecture, configuration, compiler options, linker options, and other conditions needed to reproduce each meaningful measurement.
- Compare Win32 and x64 or alternative settings using actual build output before adopting a final choice.
- Distinguish development/debug artifacts from the final submission. Do not count or exclude an artifact without stating the measurement rule.
- Do not assume that the current Debug/Release configurations or current project defaults are the final size baseline.

The solution currently exposes x86 and x64 platforms, which map to Win32 and x64 project platforms respectively. The project currently uses MSVC `v145`, Windows 10 SDK selection, C++20, Unicode, SDL checks, conformance mode, and warning level 3. The Release configurations use the Windows GUI subsystem. These describe the checked-in project state only; they are not approved final specifications.

## Data Principles

Store the minimum data and rules needed to regenerate results rather than storing fully expanded results:

- Graphic = Pattern + Palette + Transform
- Sound = Wave + Frequency + Sequence
- Map = Tile + Chunk + Rule + Seed
- Save = minimum State required for restoration, not the full derived result

Minimize disk use and deliberately trade available RAM and runtime computation for stored bytes when the measured benefit is worthwhile and game stability remains acceptable.

## Required Work Procedure

For implementation work, always proceed in this order:

`inspect current structure -> inspect relevant code -> verify whether the existing structure can be reused -> make the minimum requested implementation -> validate -> measure actual Build Size -> report results`

- Do not add unrequested features, refactors, or structural changes.
- Keep implementation files under `DeadSignal/` and keep a header beside its corresponding `.cpp` when both are genuinely needed.
- When adding a required file, update `DeadSignal.vcxproj` and `DeadSignal.vcxproj.filters` so Visual Studio includes it in the appropriate filter.
- Do not commit generated or machine-local outputs such as `.vs/`, `Debug/`, `Release/`, `x64/`, binaries, or IDE user settings.
- Use four spaces, not tabs, and follow the style already present in nearby code. Do not introduce a naming or ownership abstraction merely to enforce a generic convention.

## Validation

Validation must be proportional to the requested change and the current stage.

- Build and manually exercise only the configurations relevant to the current decision; do not treat Debug x64 and Release x64 as permanently preferred configurations.
- When architecture or build settings are being selected, compare the relevant candidates rather than assuming one.
- No separate test framework or test project is currently authorized. Add one only when concrete need and size cost have been evaluated and the user has requested or approved it.
- Report commands run, whether they succeeded, exact measured byte counts, measurement scope, and any unverified behavior.

## Status Labels for Numeric Values

Always label meaningful design and tuning values as one of:

- `확정`: use as specified by the current approved design
- `초기 기준`: use for the prototype and allow adjustment after playtesting
- `미정`: not decided; do not invent a final value

The `1,474,560 bytes` maximum for the entire final submission is `확정`. Architecture and the final compiler, CRT, language, character-set, SDL, STL, and linker choices are `미정`.

## Change and Reporting Discipline

- Inspect the actual repository state before editing.
- Limit changes to the user's explicit scope.
- Preserve unrelated user changes and machine-specific state.
- If documentation and the actual project differ, report both; do not rewrite one merely to hide the discrepancy.
- Use short, imperative commit subjects if a commit is requested. Keep unrelated changes separate.
- In a pull request, explain intent and behavior, list validation and size-measurement commands, link relevant issues, and include terminal output or screenshots when user-visible behavior changes.
